// 由 scripts/build-docs.mjs 自动生成，请勿手改。
// 内容来源：sdk/AI-CONTEXT.md、sdk/怎么用AI开发.md、sdk/README.md
// 重新生成：npm run build:docs

export interface DocToc {
  id: string;
  text: string;
  level: number;
}

export interface DocPage {
  slug: string;
  label: string;
  summary: string;
  forAi: boolean;
  file: string;
  title: string;
  html: string;
  raw: string;
  toc: DocToc[];
}

export const docPages: DocPage[] = [
  {
    "slug": "ai-context",
    "label": "给 AI 的上下文",
    "summary": "把这份整个发给 AI，它就掌握了 SDK 的全部能力边界和正确用法，不必再翻源码。",
    "forAi": true,
    "file": "AI-CONTEXT.md",
    "title": "Shroom SDK · AI 上下文",
    "html": "<blockquote>\n<p><strong>这份文件是给 AI 读的。</strong> 你（AI）拿到它之后，就掌握了这个 SDK 的全部能力边界和\n正确用法，可以直接按用户的需求写代码，不需要再去翻源码。</p>\n<p>人类用户：把这个文件整个丢给 AI，然后描述你想做的东西就行。</p>\n</blockquote>\n<hr>\n<h2 id=\"s1\">0. 一句话</h2>\n<p>Shroom SDK 把一块压力传感器阵列接进 JS：<strong>连上串口 → 每秒几十上百次拿到一个数值矩阵 → 画成图</strong>。</p>\n<p>它<strong>只做这三件事</strong>。曲线、回放、报表、手势识别、算法、数据库——<strong>一律不在 SDK 里</strong>，\n那些正是你要帮用户写的部分。</p>\n<p><strong>不要</strong>去 SDK 里找这些东西，它们不存在：\n标定、kPa/N 等物理单位换算、滤波器、峰值检测、手势分类、录制回放、\n多设备管理、蓝牙、WiFi、云端上传、UI 组件库。</p>\n<hr>\n<h2 id=\"s2\">0.5 先确定 import 路径（第一行代码就会错）</h2>\n<p>本文所有示例都写成 <code>&#39;./sdk/web/index.js&#39;</code>，那是<strong>你的文件在 <code>sdk/</code> 外面</strong>时的写法。\n<strong>先搞清楚你要把文件放在哪</strong>，照下表选：</p>\n<table>\n<thead>\n<tr>\n<th>你的文件放在</th>\n<th>import 写法</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>sdk/web/</code> 里面（<strong>最常见</strong>，因为要用自带服务器打开）</td>\n<td><code>&#39;./index.js&#39;</code></td>\n</tr>\n<tr>\n<td><code>sdk/</code> 的同级目录</td>\n<td><code>&#39;./sdk/web/index.js&#39;</code></td>\n</tr>\n<tr>\n<td>项目里任意位置</td>\n<td>按相对层级算，目标永远是 <code>sdk/web/index.js</code></td>\n</tr>\n</tbody></table>\n<p><strong>为什么最常见的是第一种</strong>：浏览器串口不能用 <code>file://</code>（见 §5.2），\n用户一般用 SDK 自带的 <code>start-demo.bat</code> / <code>start.mjs</code> 起服务，那个服务器的根目录就是 <code>sdk/</code>，\n所以把页面放进 <code>sdk/web/</code> 是最省事的做法，访问 <code>http://localhost:5178/你的文件.html</code> 即可。</p>\n<blockquote>\n<p>路径写错的症状：<strong>页面全白，Console 里一条 404</strong>。这是最高频的低级错误，\n写第一行之前先确认好。</p>\n</blockquote>\n<hr>\n<h2 id=\"s3\">1. 能力边界（先读这段，能省掉一半错误）</h2>\n<table>\n<thead>\n<tr>\n<th>你想要的</th>\n<th>SDK 里有吗</th>\n<th>怎么办</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>连接串口拿数据</td>\n<td>✅ 有</td>\n<td><code>Shroom.connect()</code></td>\n</tr>\n<tr>\n<td>没硬件也能开发</td>\n<td>✅ 有</td>\n<td><code>Shroom.mock()</code>，接口完全一样</td>\n</tr>\n<tr>\n<td>画成热力图 / 点阵</td>\n<td>✅ 有</td>\n<td><code>Shroom.createHeatmap()</code></td>\n</tr>\n<tr>\n<td>压力重心、受力面积、最大值</td>\n<td>✅ 有</td>\n<td>每帧算好了，直接读 <code>frame.center</code> / <code>frame.area</code> / <code>frame.max</code></td>\n</tr>\n<tr>\n<td>换算成 kPa / 牛顿</td>\n<td>❌ 没有</td>\n<td><strong>做不到</strong>，见 §3 的警告</td>\n</tr>\n<tr>\n<td>曲线图 / 折线图</td>\n<td>❌ 没有</td>\n<td>你自己攒 <code>frame.max</code> 进数组，用 Chart.js / canvas 画</td>\n</tr>\n<tr>\n<td>录制与回放</td>\n<td>❌ 没有</td>\n<td>你自己把 <code>frame.raw</code> 存下来，回放时再 <code>decodeFrame()</code></td>\n</tr>\n<tr>\n<td>手势识别 / 姿态分类</td>\n<td>❌ 没有</td>\n<td>你自己在 <code>onFrame</code> 里写逻辑</td>\n</tr>\n<tr>\n<td>滤波、去噪</td>\n<td>❌ 没有</td>\n<td>你自己对 <code>frame.values</code> 处理（SDK 只在<strong>显示</strong>时做了柔化，数据是原始的）</td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"s4\">2. 三十秒上手</h2>\n<p>浏览器（最常见）：</p>\n<pre><code class=\"language-js\">import { Shroom } from &#39;./sdk/web/index.js&#39;;\n\nconst heatmap = Shroom.createHeatmap(&#39;#view&#39;);       // #view 是一个 &lt;canvas&gt;\n\ndocument.querySelector(&#39;#btn&#39;).onclick = async () =&gt; {\n  const device = await Shroom.connect();             // ⚠️ 必须在点击回调里\n  device.onFrame((frame) =&gt; heatmap.render(frame));\n};\n</code></pre>\n<p>Node / Electron：</p>\n<pre><code class=\"language-js\">import { Shroom } from &#39;./sdk/node/index.js&#39;;        // 需要先 npm i serialport\n\nconst device = await Shroom.connect({ path: &#39;COM3&#39; });\ndevice.onFrame((frame) =&gt; console.log(Shroom.renderAscii(frame)));\n</code></pre>\n<p>没有硬件时把 <code>Shroom.connect(...)</code> 换成 <code>Shroom.mock({ rows: 32, cols: 32, fps: 30 })</code>，\n<strong>其余代码一个字都不用改</strong>。写新功能时建议一律先用 mock 调通。</p>\n<blockquote>\n<p><strong><code>mock()</code> 是同步的</strong>（<code>connect()</code> 是 async）。写成 <code>await Shroom.mock()</code> 也不会出错——\n<code>await</code> 一个非 Promise 值就是它本身——所以两种写法都能跑。</p>\n</blockquote>\n<h3 id=\"s5\">mock 产生的是什么数据</h3>\n<p><strong>两个高斯亮斑绕着中心转</strong>（一大一小，反向旋转），外加一点底噪：</p>\n<ul>\n<li>峰值 <code>max</code> 稳定在 <strong>0.9 以上</strong>，底噪约 0.01</li>\n<li>亮斑<strong>持续移动</strong>，<code>center</code> 一直在变，绕中心画圈</li>\n<li>默认 32×32、30fps</li>\n</ul>\n<p><strong>它能验证什么</strong>：渲染、<code>center</code> 跟随、<code>max</code>/<code>area</code> 读数、曲线、录制回放——链路整体是通的。</p>\n<p><strong>它不能验证什么</strong>：</p>\n<ul>\n<li><strong>真实的按压节奏</strong>。mock 的 <code>max</code> 一直很高、从不「松手」，所以**「按下→抬起」这类状态机在 mock 下不会触发**。\n测这类逻辑要么手动造数据，要么就得插真设备。</li>\n<li>你的传感器实际的数值范围和噪声水平。</li>\n</ul>\n<hr>\n<h2 id=\"s6\">3. Frame：唯一的数据结构</h2>\n<p>不管数据从哪来（浏览器串口 / Node 串口 / 模拟），<code>onFrame</code> 拿到的都是这个：</p>\n<pre><code class=\"language-ts\">interface Frame {\n  raw: Uint8Array;        // 原始 ADC 值 0~255，一个点一个字节，长度 = 实际收到的点数\n  values: Float32Array;   // 归一化 0~1，长度 = rows * cols  ← 你几乎只用这个\n  rows: number;           // 矩阵行数\n  cols: number;           // 矩阵列数\n  min: number;            // values 的最小值\n  max: number;            // values 的最大值   ← 「按得多重」通常看它\n  avg: number;            // values 的平均值\n  area: number;           // 超过阈值(默认 0.02)的点数 ← 「接触面积有多大」看它\n  center: { x: number; y: number };  // 压力重心，x/y 都是 0~1，没受力时是 {0.5, 0.5}\n  timestamp: number;      // Date.now() 毫秒\n}\n</code></pre>\n<p>按行列取某个点：</p>\n<pre><code class=\"language-js\">const v = frame.values[y * frame.cols + x];   // 0~1\n</code></pre>\n<h3 id=\"s7\">坐标系（做位置相关功能前必读）</h3>\n<p>数据的排布方式是确定的，<strong>不受任何显示选项影响</strong>：</p>\n<pre><code>values 索引 = y * cols + x      x 是列（0 ~ cols-1），y 是行（0 ~ rows-1）\n\ncenter.x = 列方向，0 = 第 0 列，1 = 最后一列\ncenter.y = 行方向，0 = 第 0 行，1 = 最后一行\n</code></pre>\n<p>把 <code>center</code> 映射成 N×N 的格子（记得钳制，浮点可能取到边界）：</p>\n<pre><code class=\"language-js\">const col = Math.min(N - 1, Math.floor(frame.center.x * N));\nconst row = Math.min(N - 1, Math.floor(frame.center.y * N));\n</code></pre>\n<blockquote>\n<p><strong><code>flipY</code> 不影响 <code>center</code>。</strong> <code>flipY</code> 只是 <code>&#39;dots&#39;</code> 模式<strong>画图时</strong>把行序颠倒一下，\n<code>frame.values</code> 和 <code>frame.center</code> 永远是原始行序。所以你的位置判定逻辑不用管 <code>flipY</code>。</p>\n<p><strong>但传感器实物朝哪一边放，SDK 不知道。</strong> 第 0 行是贴近用户的那边还是远离的那边，\n取决于设备怎么摆。<strong>做位置类功能时给用户一个「上下翻转」开关</strong>（<code>y → 1 - y</code>），\n比你猜一个方向可靠。</p>\n</blockquote>\n<blockquote>\n<p><strong>多点按压时 <code>center</code> 是加权平均。</strong> 两个手指分别按左上和右下，重心会落在正中间——\n那里可能根本没人按。SDK <strong>没有</strong>提供「最大压力点的坐标」，需要的话自己遍历 <code>values</code> 找最大值下标：</p>\n<pre><code class=\"language-js\">let peak = 0, peakIdx = 0;\nfor (let i = 0; i &lt; frame.values.length; i += 1) {\n  if (frame.values[i] &gt; peak) { peak = frame.values[i]; peakIdx = i; }\n}\nconst px = peakIdx % frame.cols, py = Math.floor(peakIdx / frame.cols);\n</code></pre>\n<p>单点触发类应用（按格子、打地鼠）用峰值点通常比用重心准。</p>\n</blockquote>\n<h3 id=\"s8\">典型数值量级（写阈值时的依据）</h3>\n<p>传感器逐片有差异，下面是<strong>数量级参考</strong>，不是标准值：</p>\n<table>\n<thead>\n<tr>\n<th>状态</th>\n<th><code>frame.max</code> 大致范围</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>没人碰</td>\n<td>0.01 ~ 0.05（底噪）</td>\n</tr>\n<tr>\n<td>手指轻触</td>\n<td>0.1 ~ 0.3</td>\n</tr>\n<tr>\n<td>手指正常按</td>\n<td>0.3 ~ 0.7</td>\n</tr>\n<tr>\n<td>用力按 / 手掌压</td>\n<td>0.7 ~ 1.0</td>\n</tr>\n</tbody></table>\n<p>所以「<strong>判定为按下</strong>」的阈值取 <strong>0.15 左右</strong>是个合理起点，<code>0.05</code> 只够用来判断「有没有人碰」。</p>\n<p><strong>更稳的做法是不写死阈值</strong>，开头采一秒基线，然后按相对量触发：</p>\n<pre><code class=\"language-js\">// 让用户别碰传感器，先量一下底噪\nlet baseline = 0.03;\nsetTimeout(() =&gt; { baseline = observedMax; }, 1000);\n\nconst pressed = frame.max &gt; baseline + 0.12;\n</code></pre>\n<p>按压检测建议用<strong>双阈值</strong>，避免在临界点反复触发：</p>\n<pre><code class=\"language-js\">if (!isDown &amp;&amp; frame.max &gt; 0.15) isDown = true;        // 按下\nelse if (isDown &amp;&amp; frame.max &lt; 0.08) isDown = false;   // 抬起（阈值更低）\n</code></pre>\n<blockquote>\n<h3 id=\"s9\">⚠️ 关于单位：<code>values</code> 是相对值，不是物理量</h3>\n<p><code>values</code> 是 ADC 读数除以满量程得到的 <strong>0~1 相对值</strong>。它<strong>不是</strong> kPa、不是牛顿、不是克。</p>\n<p>要换算成物理单位需要每一台设备各自的标定曲线（传感器是非线性的，且逐片有差异），\n这些数据<strong>不在这个 SDK 里，也不会在</strong>。</p>\n<p>所以：</p>\n<ul>\n<li>✅ 可以说「压力 0.72」「比刚才重了」「重心往左移了」</li>\n<li>❌ <strong>不要</strong>在 UI 上写 kPa / N / kg，<strong>不要</strong>自己编一个系数去乘</li>\n<li>如果用户明确要物理单位，告诉他需要找设备厂商拿标定数据，这是 SDK 之外的事</li>\n</ul>\n</blockquote>\n<p><code>raw</code> 和 <code>values</code> 的长度可能不一致：<code>raw</code> 是实际收到的字节数，\n<code>values</code> 长度恒为 <code>rows * cols</code>，不够的位置补 0。日常用 <code>values</code> 就行。</p>\n<hr>\n<h2 id=\"s10\">4. 完整 API</h2>\n<h3 id=\"s11\">4.1 浏览器入口 <code>sdk/web/index.js</code></h3>\n<pre><code class=\"language-js\">Shroom.connect(options?)              // → Promise&lt;Device&gt;，必须在用户手势里调用\nShroom.mock(options?)                 // → Device，模拟数据\nShroom.createHeatmap(target, opts?)   // → Heatmap\nShroom.isSupported()                  // → boolean，当前浏览器能不能用串口\nShroom.version                        // &#39;0.1.0&#39;\n</code></pre>\n<p>也可以按名字导入，还额外导出了底层件（自己接别的数据源时用）：</p>\n<pre><code class=\"language-js\">import {\n  connectSerial, isSerialSupported, createHeatmap, createMockDevice,\n  createFramer, decodeFrame, resolveShape, DEFAULT_DELIMITER,\n  jet, jetWhite, grey, getColormap,\n} from &#39;./sdk/web/index.js&#39;;\n</code></pre>\n<h3 id=\"s12\">4.2 Node 入口 <code>sdk/node/index.js</code></h3>\n<pre><code class=\"language-js\">Shroom.connect(options?)     // → Promise&lt;Device&gt;，options.path 不填就自动挑第一个串口\nShroom.listPorts()           // → Promise&lt;PortInfo[]&gt;，列出机器上的串口\nShroom.mock(options?)        // → Device\nShroom.renderAscii(frame, o?) // → string，终端里的彩色方块图，直接 console.log\n</code></pre>\n<p>需要 <code>npm i serialport</code>（原生模块，按需加载；不装也不影响 mock 和 core）。</p>\n<h3 id=\"s13\">4.3 Device（连接后拿到的东西，两端一致）</h3>\n<pre><code class=\"language-js\">device.info            // { source: &#39;web-serial&#39;|&#39;node-serial&#39;|&#39;mock&#39;, rows, cols, baudRate|path|fps }\ndevice.onFrame(fn)     // 订阅每一帧，返回一个「取消订阅」的函数\nawait device.close()   // 断开\n\n// 下面四个只有真实串口设备有，mock 没有（用 ?. 或先判断 source）\ndevice.bytesReceived   // 收到的原始字节总数\ndevice.frameCount      // 成功切出的帧数\ndevice.droppedCount    // 被丢掉的脏帧数\ndevice.frameLength     // 锁定下来的帧长（字节），还没锁上是 0\n</code></pre>\n<p><code>onFrame</code> 可以订阅多个；一个回调抛错不影响其他回调。取消订阅：</p>\n<pre><code class=\"language-js\">const off = device.onFrame(handler);\noff();   // 不再收\n</code></pre>\n<blockquote>\n<p><strong><code>close()</code> 会自动清掉所有订阅</strong>，不用先挨个 <code>off()</code> 再 <code>close()</code>。\n<code>off()</code> 只在「设备还连着，但某个模块不想再收了」时才需要。</p>\n</blockquote>\n<h3 id=\"s14\">4.3.1 connect() 抛错的两种情况</h3>\n<p>用户在串口选择框上点「取消」也会抛错，<strong>要和真正的连接失败分开处理</strong>，\n否则用户取消一下就看到一句「连接失败」，很莫名其妙：</p>\n<pre><code class=\"language-js\">try {\n  device = await Shroom.connect();\n} catch (err) {\n  // 用户主动取消：静默返回，什么都不用提示\n  if (err?.name === &#39;NotFoundError&#39;) return;\n  // 其余才是真的出问题了\n  showError(&#39;连接失败：&#39; + err.message);\n}\n</code></pre>\n<h3 id=\"s15\">4.3.2 切换设备时的顺序（容易踩）</h3>\n<p>必须<strong>先 connect，成功之后再关旧的</strong>。反过来写会让第二次连接直接失败——\n因为 <code>await oldDevice.close()</code> 会消耗掉用户手势（见 §5.1）：</p>\n<pre><code class=\"language-js\">// ✅ 对\nbutton.onclick = async () =&gt; {\n  const next = await Shroom.connect();   // 先连新的\n  await current?.close();                // 再关旧的\n  current = next;\n};\n\n// ❌ 错：第二次点就连不上了\nbutton.onclick = async () =&gt; {\n  await current?.close();                // ← 这一行让手势失效\n  current = await Shroom.connect();      // 抛错\n};\n</code></pre>\n<h3 id=\"s16\">4.4 连接参数</h3>\n<pre><code class=\"language-js\">await Shroom.connect({\n  baudRate: 1000000,                    // 默认 1000000\n  delimiter: [0xAA, 0x55, 0x03, 0x99],  // 帧分隔符，默认就是这个\n  rows: 32, cols: 32,                   // 不填按方阵推断：1024 个点 → 32×32\n  fullScale: 255,                       // 满量程，默认 255\n  threshold: 0.02,                      // 算 area / center 的有效点阈值\n  lockLength: true,                     // 锁定帧长，默认开，见 §6\n  filters: [{ usbVendorId: 0x1a86 }],   // 仅浏览器：过滤设备选择框\n  path: &#39;COM3&#39;,                         // 仅 Node：端口号\n});\n</code></pre>\n<h3 id=\"s17\">4.5 Heatmap</h3>\n<pre><code class=\"language-js\">const heatmap = Shroom.createHeatmap(&#39;#view&#39;, { mode: &#39;dots&#39; });\nheatmap.render(frame);          // 记下这一帧，下个屏幕刷新周期画出来\nheatmap.setOptions({ gain: 2 }); // 运行中改任何选项\nheatmap.resize();               // 容器尺寸变了时调（一般不用手动调）\nheatmap.clear();                // 清空\nheatmap.canvas;                 // 拿到底层 &lt;canvas&gt;\n</code></pre>\n<p>选项：</p>\n<table>\n<thead>\n<tr>\n<th>选项</th>\n<th>默认</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>mode</code></td>\n<td><code>&#39;dots&#39;</code></td>\n<td><code>&#39;dots&#39;</code> 透视点阵 / <code>&#39;heat&#39;</code> 热力圆斑 / <code>&#39;grid&#39;</code> 方格</td>\n</tr>\n<tr>\n<td><code>colormap</code></td>\n<td><code>&#39;jet&#39;</code></td>\n<td><code>&#39;jet&#39;</code> / <code>&#39;jetWhite&#39;</code> / <code>&#39;grey&#39;</code>，或自定义函数 <code>(t) =&gt; [r,g,b]</code>，t 是 0~1</td>\n</tr>\n<tr>\n<td><code>gain</code></td>\n<td><code>1</code></td>\n<td>显示增益，信号弱时调大。<strong>只影响显示，不改 <code>frame.values</code></strong></td>\n</tr>\n<tr>\n<td><code>dotSize</code></td>\n<td><code>1</code></td>\n<td>点 / 圆斑大小倍数</td>\n</tr>\n<tr>\n<td><code>smooth</code></td>\n<td><code>true</code></td>\n<td>是否平滑，只对 <code>&#39;heat&#39;</code> / <code>&#39;grid&#39;</code> 有效</td>\n</tr>\n<tr>\n<td><code>relief</code></td>\n<td><code>1</code></td>\n<td>点阵起伏高度倍数，给 <code>0</code> 就是平的俯视点阵。只对 <code>&#39;dots&#39;</code> 有效</td>\n</tr>\n<tr>\n<td><code>tilt</code></td>\n<td><code>70</code></td>\n<td>俯视角（度，5~89）。越大越像正俯视、方阵看着越方。只对 <code>&#39;dots&#39;</code> 有效</td>\n</tr>\n<tr>\n<td><code>flipY</code></td>\n<td><code>true</code></td>\n<td>数据第 0 行画在近处（画面下方）。<strong>按下面却是上面鼓起来就设成 <code>false</code></strong>。只对 <code>&#39;dots&#39;</code> 有效</td>\n</tr>\n</tbody></table>\n<p><code>render()</code> 只是<strong>记下</strong>这一帧，实际绘制推到下一个屏幕刷新周期。所以哪怕串口一秒来 100 帧，\n也只会画 60 次，不用你自己做节流。</p>\n<blockquote>\n<p><strong>canvas 尺寸交给 CSS 就行。</strong> heatmap 内部按 <code>clientWidth/clientHeight × devicePixelRatio</code>\n自己设置画布分辨率，高分屏不会糊。你只要给 canvas 一个 CSS 尺寸：</p>\n<pre><code class=\"language-html\">&lt;canvas id=&quot;view&quot; style=&quot;width: 360px; height: 360px&quot;&gt;&lt;/canvas&gt;\n</code></pre>\n<p><strong>不要</strong>自己去写 <code>canvas.width = 360</code>，会被覆盖。容器尺寸变化一般能自动跟上，\n极端情况下手动调一次 <code>heatmap.resize()</code>。</p>\n</blockquote>\n<h3 id=\"s18\">4.6 底层件（自己接数据源时用）</h3>\n<p>想接 WebSocket、蓝牙、文件回放等 SDK 没提供的数据源，用这两个就能拿到同样的 Frame：</p>\n<pre><code class=\"language-js\">import { createFramer, decodeFrame } from &#39;./sdk/web/index.js&#39;;\n\nconst framer = createFramer();                    // 字节流 → 一帧一帧\nfor (const payload of framer.push(someBytes)) {   // push 返回本次切出的所有帧\n  const frame = decodeFrame(payload, { rows: 32, cols: 32 });\n  heatmap.render(frame);\n}\n</code></pre>\n<p><code>createFramer()</code> 返回 <code>{ push(chunk), reset(), droppedCount, frameLength }</code>。</p>\n<hr>\n<h2 id=\"s19\">5. 硬性约束（违反了代码一定跑不起来）</h2>\n<h3 id=\"s20\">5.1 浏览器串口必须在用户手势里调用</h3>\n<pre><code class=\"language-js\">// ✅ 对\nbutton.onclick = async () =&gt; { const d = await Shroom.connect(); };\n\n// ❌ 错：页面一加载就连，浏览器直接拒绝\nwindow.onload = async () =&gt; { const d = await Shroom.connect(); };\n\n// ❌ 错：await 了别的东西之后再 connect，用户手势已经过期\nbutton.onclick = async () =&gt; {\n  await fetch(&#39;/api/config&#39;);       // ← 这一行让手势失效\n  const d = await Shroom.connect(); // 抛错\n};\n</code></pre>\n<p>要先取配置就<strong>先 connect 再 fetch</strong>，或者把配置提前准备好。</p>\n<h3 id=\"s21\">5.2 浏览器串口只在 https 或 localhost 下开放</h3>\n<p><code>navigator.serial</code> 标了 <code>SecureContext</code>。所以：</p>\n<ul>\n<li><code>http://localhost:xxxx</code> ✅</li>\n<li><code>https://你的域名</code> ✅</li>\n<li><code>file://</code>（双击 html 文件打开）❌ <strong>永远不行</strong>，任何打包方式都绕不过</li>\n<li><code>http://192.168.x.x</code> ❌</li>\n</ul>\n<p>写代码时用 <code>Shroom.isSupported()</code> 提前判断，给用户一个明确的提示，别让按钮点了没反应。</p>\n<h3 id=\"s22\">5.3 只有 Chrome / Edge 支持</h3>\n<p>Safari 和 Firefox 没有 Web Serial，且短期内不会有。需要跨浏览器就走 Node/Electron 方案。</p>\n<p><strong>操作系统不是限制</strong>：Windows / macOS / Linux 三个桌面系统上的 Chrome / Edge 都能用，\n代码一份，不用写平台分支。移动端不行（Android Chrome 也没有 Web Serial）。</p>\n<p>写给用户看的提示文案里，<strong>别写「请在 Windows 上运行」</strong>——限制是浏览器，不是系统。</p>\n<h3 id=\"s23\">5.4 帧率很高，回调里别做重活</h3>\n<p>一秒可能来 100 帧。<code>onFrame</code> 里做 DOM 操作、<code>JSON.stringify</code>、\n<code>console.log</code> 大对象都会卡。要更新 UI 文字就自己降频：</p>\n<pre><code class=\"language-js\">let last = 0;\ndevice.onFrame((frame) =&gt; {\n  heatmap.render(frame);                    // 这个内部已经按屏幕刷新率节流了\n  if (frame.timestamp - last &gt; 100) {       // 文字每 100ms 更新一次就够了\n    last = frame.timestamp;\n    label.textContent = frame.max.toFixed(3);\n  }\n});\n</code></pre>\n<h3 id=\"s24\">5.5 别持有 frame 对象</h3>\n<p><code>frame.raw</code> 是原始 buffer 的视图，<code>frame.values</code> 每帧都是新的。要存起来做回放/曲线，\n<strong>存副本或存标量</strong>，别把整个 frame 塞进数组：</p>\n<pre><code class=\"language-js\">// ✅ 存标量\nhistory.push(frame.max);\n\n// ✅ 存副本\nrecorded.push(new Uint8Array(frame.raw));\n\n// ❌ 存引用，内存会炸，raw 还可能被复用\nrecorded.push(frame);\n</code></pre>\n<hr>\n<h2 id=\"s25\">6. 帧长锁定（<code>lockLength</code>，默认开）</h2>\n<p>分隔符只有 4 字节，数据里迟早会撞出一串一模一样的，于是切出长度不对的脏帧。\n切帧器默认会锁定帧长把这些丢掉，也能自动纠回来。</p>\n<p><strong>你只需要知道一件事：<code>droppedCount</code> 有个小数字在缓慢增长是正常的，不用报错给用户。</strong></p>\n<p>（真需要收变长帧就传 <code>lockLength: false</code>。）</p>\n<hr>\n<h2 id=\"s26\">7. 排错对照表</h2>\n<p>用户说「连上了但没画面」，让他看这三个数：</p>\n<table>\n<thead>\n<tr>\n<th>现象</th>\n<th>原因</th>\n<th>怎么办</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>bytesReceived === 0</code></td>\n<td>设备根本没在发数据</td>\n<td>选错 COM 口 / 线坏了 / 设备要先发启动指令</td>\n</tr>\n<tr>\n<td>字节在涨但 <code>frameCount === 0</code></td>\n<td>波特率或分隔符不对</td>\n<td>依次试 <code>1000000</code>（默认）、<code>921600</code>、<code>1500000</code>、<code>3000000</code>、<code>115200</code></td>\n</tr>\n<tr>\n<td>帧在涨但画面是一条横线</td>\n<td>帧长不是完全平方数，推不出方阵</td>\n<td>显式传 <code>rows</code> / <code>cols</code>；<code>device.frameLength</code> 就是实际帧长</td>\n</tr>\n<tr>\n<td>帧在涨但画面花</td>\n<td>矩阵尺寸猜错了</td>\n<td>同上，显式传 <code>rows</code> / <code>cols</code></td>\n</tr>\n<tr>\n<td><code>droppedCount</code> 缓慢增长</td>\n<td>假分隔符被帧长锁定挡掉了</td>\n<td>正常，不用管</td>\n</tr>\n<tr>\n<td><code>droppedCount</code> 猛涨</td>\n<td>波特率不稳</td>\n<td>换一档波特率</td>\n</tr>\n<tr>\n<td>点阵按下面却是上面鼓起来</td>\n<td>传感器行序和默认相反</td>\n<td><code>heatmap.setOptions({ flipY: false })</code></td>\n</tr>\n<tr>\n<td>连接按钮点了没反应</td>\n<td>不在用户手势里 / 不是安全上下文</td>\n<td>见 §5.1、§5.2</td>\n</tr>\n<tr>\n<td>浏览器弹了选择框但列表是空的</td>\n<td>设备没插好，或系统缺驱动 / 缺权限</td>\n<td>见下面这行</td>\n</tr>\n<tr>\n<td>Node 端 <code>Permission denied</code> 或 <code>listPorts()</code> 返回空</td>\n<td>Linux 上普通用户默认没有串口权限</td>\n<td><code>sudo usermod -aG dialout $USER</code> 后重新登录；设备名一般是 <code>/dev/ttyUSB0</code></td>\n</tr>\n</tbody></table>\n<hr>\n<h2 id=\"s27\">8. 可以直接抄的例子</h2>\n<h3 id=\"s28\">8.1 用重心控制一个小球</h3>\n<pre><code class=\"language-js\">const heatmap = Shroom.createHeatmap(&#39;#view&#39;);\nconst ball = document.querySelector(&#39;#ball&#39;);\n\ndevice.onFrame((frame) =&gt; {\n  heatmap.render(frame);\n  if (frame.max &lt; 0.05) return;                   // 没人按就别动\n  ball.style.left = frame.center.x * 100 + &#39;%&#39;;\n  ball.style.top  = frame.center.y * 100 + &#39;%&#39;;\n  ball.style.background = frame.max &gt; 0.5 ? &#39;red&#39; : &#39;white&#39;;\n});\n</code></pre>\n<h3 id=\"s29\">8.2 按到哪个格子（打地鼠这类）</h3>\n<p>把传感器分成 N×N 格，检测「按下 → 抬起」并判定落在哪一格：</p>\n<pre><code class=\"language-js\">const N = 3;\nlet isDown = false, peakMax = 0, peakCenter = null;\n\ndevice.onFrame((frame) =&gt; {\n  heatmap.render(frame);\n\n  if (!isDown &amp;&amp; frame.max &gt; 0.15) {           // 按下\n    isDown = true; peakMax = 0;\n  }\n  if (isDown) {\n    if (frame.max &gt; peakMax) {                 // 记住最重那一刻的位置\n      peakMax = frame.max;\n      peakCenter = { x: frame.center.x, y: frame.center.y };   // 存副本\n    }\n    if (frame.max &lt; 0.08) {                    // 抬起，结算\n      isDown = false;\n      const col = Math.min(N - 1, Math.floor(peakCenter.x * N));\n      const row = Math.min(N - 1, Math.floor(peakCenter.y * N));\n      onHit(row, col);                         // ← 你的逻辑\n    }\n  }\n});\n</code></pre>\n<p>两个要点：<strong>取按压过程中最重那一帧的位置</strong>（刚碰到和快松手时重心会飘），\n以及<strong>双阈值</strong>（0.15 按下 / 0.08 抬起）避免在临界点反复触发。</p>\n<blockquote>\n<p>mock 数据 <code>max</code> 一直很高、不会「松手」，<strong>这段逻辑在 mock 下不会触发</strong>，需要真设备测。</p>\n</blockquote>\n<h3 id=\"s30\">8.3 最大压力曲线</h3>\n<pre><code class=\"language-js\">const history = [];\ndevice.onFrame((frame) =&gt; {\n  history.push(frame.max);                        // 存标量，不是整个 frame\n  if (history.length &gt; 300) history.shift();      // 只留最近 300 点\n});\n\nsetInterval(() =&gt; drawCurve(history), 50);        // 画图跟采集分开，别在 onFrame 里画\n</code></pre>\n<h3 id=\"s31\">8.4 录制与回放</h3>\n<pre><code class=\"language-js\">import { decodeFrame } from &#39;./sdk/web/index.js&#39;;\n\nconst tape = [];\nconst off = device.onFrame((f) =&gt; tape.push(new Uint8Array(f.raw)));  // 存副本\n\n// 停止录制\noff();\n\n// 回放\nlet i = 0;\nconst timer = setInterval(() =&gt; {\n  if (i &gt;= tape.length) return clearInterval(timer);\n  heatmap.render(decodeFrame(tape[i++], { rows: 32, cols: 32 }));\n}, 33);\n</code></pre>\n<h3 id=\"s32\">8.5 分区域统计（比如把 32×32 分成四象限）</h3>\n<pre><code class=\"language-js\">function quadrantSums(frame) {\n  const { values, rows, cols } = frame;\n  const q = [0, 0, 0, 0];\n  for (let y = 0; y &lt; rows; y += 1) {\n    for (let x = 0; x &lt; cols; x += 1) {\n      const idx = (y &lt; rows / 2 ? 0 : 2) + (x &lt; cols / 2 ? 0 : 1);\n      q[idx] += values[y * cols + x];\n    }\n  }\n  return q;    // [左上, 右上, 左下, 右下]\n}\n</code></pre>\n<h3 id=\"s33\">8.6 简单去噪（SDK 不提供，自己两行）</h3>\n<pre><code class=\"language-js\">let smoothed = null;\ndevice.onFrame((frame) =&gt; {\n  if (!smoothed || smoothed.length !== frame.values.length) {\n    smoothed = new Float32Array(frame.values);\n  }\n  for (let i = 0; i &lt; smoothed.length; i += 1) {\n    smoothed[i] += (frame.values[i] - smoothed[i]) * 0.3;   // 越小越黏\n  }\n  // 拿 smoothed 去做你的判断\n});\n</code></pre>\n<hr>\n<h2 id=\"s34\">9. 目录结构（主要用来确认 import 路径）</h2>\n<pre><code>sdk/\n├─ core/              纯逻辑，浏览器和 Node 共用\n│  ├─ framer.js         字节流 → 一帧一帧\n│  ├─ frame.js          一帧字节 → Frame 对象\n│  ├─ colormap.js       0~1 → [r,g,b]\n│  ├─ device.js         帧订阅中心\n│  └─ mock.js           模拟数据源\n├─ web/               浏览器\n│  ├─ index.js          ← 浏览器入口，import 这个\n│  ├─ serial.js         Web Serial 连接\n│  ├─ heatmap.js        canvas 渲染\n│  ├─ index.html        官方示例页\n│  └─ shroom.bundle.js  单文件版，&lt;script&gt; 直接引\n├─ node/               Node / Electron\n│  └─ index.js          ← Node 入口，import 这个\n├─ start.mjs          本地服务器（根目录 = sdk/，端口 5178 起，占用则顺延到 5182）\n└─ index.d.ts         TypeScript 类型定义\n</code></pre>\n<p><strong>你只需要 import 两个入口之一</strong>：<code>web/index.js</code> 或 <code>node/index.js</code>，\n路径怎么写见 §0.5。其余文件不用读，本文已经涵盖了它们的全部对外行为。</p>\n<blockquote>\n<p>唯一的例外是 §4.6 的 <code>createFramer</code> / <code>decodeFrame</code>——接非串口数据源时才用，\n它们同样从入口文件导出，不用去 <code>core/</code> 里单独引。</p>\n</blockquote>\n<hr>\n<h2 id=\"s35\">10. 写代码时的默认选择</h2>\n<p>除非用户另有要求，按这个来：</p>\n<ul>\n<li><strong>先用 <code>Shroom.mock()</code> 把功能跑通</strong>，最后再换成 <code>Shroom.connect()</code>。用户往往手边没设备。</li>\n<li>浏览器方案优先，除非用户明确说要 Electron / 后端 / 命令行。</li>\n<li>连接按钮的文案和禁用状态要处理好：先 <code>Shroom.isSupported()</code> 判断，\n不支持就禁用按钮并说明原因（是 <code>file://</code>？还是浏览器不对？），别让用户点了没反应。</li>\n<li>数值一律按 0~1 显示，或者显示成百分比，<strong>不要编造物理单位</strong>。</li>\n<li><code>onFrame</code> 里只做计算和 <code>heatmap.render()</code>，DOM 更新降频到 10Hz 左右。</li>\n<li>需要本地起服务时提醒用户：不能双击 html 文件，要 <code>node start.mjs</code>（SDK 里自带）。</li>\n</ul>\n",
    "raw": "# Shroom SDK · AI 上下文\n\n> **这份文件是给 AI 读的。** 你（AI）拿到它之后，就掌握了这个 SDK 的全部能力边界和\n> 正确用法，可以直接按用户的需求写代码，不需要再去翻源码。\n>\n> 人类用户：把这个文件整个丢给 AI，然后描述你想做的东西就行。\n\n---\n\n## 0. 一句话\n\nShroom SDK 把一块压力传感器阵列接进 JS：**连上串口 → 每秒几十上百次拿到一个数值矩阵 → 画成图**。\n\n它**只做这三件事**。曲线、回放、报表、手势识别、算法、数据库——**一律不在 SDK 里**，\n那些正是你要帮用户写的部分。\n\n**不要**去 SDK 里找这些东西，它们不存在：\n标定、kPa/N 等物理单位换算、滤波器、峰值检测、手势分类、录制回放、\n多设备管理、蓝牙、WiFi、云端上传、UI 组件库。\n\n---\n\n## 0.5 先确定 import 路径（第一行代码就会错）\n\n本文所有示例都写成 `'./sdk/web/index.js'`，那是**你的文件在 `sdk/` 外面**时的写法。\n**先搞清楚你要把文件放在哪**，照下表选：\n\n| 你的文件放在 | import 写法 |\n| --- | --- |\n| `sdk/web/` 里面（**最常见**，因为要用自带服务器打开） | `'./index.js'` |\n| `sdk/` 的同级目录 | `'./sdk/web/index.js'` |\n| 项目里任意位置 | 按相对层级算，目标永远是 `sdk/web/index.js` |\n\n**为什么最常见的是第一种**：浏览器串口不能用 `file://`（见 §5.2），\n用户一般用 SDK 自带的 `start-demo.bat` / `start.mjs` 起服务，那个服务器的根目录就是 `sdk/`，\n所以把页面放进 `sdk/web/` 是最省事的做法，访问 `http://localhost:5178/你的文件.html` 即可。\n\n> 路径写错的症状：**页面全白，Console 里一条 404**。这是最高频的低级错误，\n> 写第一行之前先确认好。\n\n---\n\n## 1. 能力边界（先读这段，能省掉一半错误）\n\n| 你想要的 | SDK 里有吗 | 怎么办 |\n| --- | --- | --- |\n| 连接串口拿数据 | ✅ 有 | `Shroom.connect()` |\n| 没硬件也能开发 | ✅ 有 | `Shroom.mock()`，接口完全一样 |\n| 画成热力图 / 点阵 | ✅ 有 | `Shroom.createHeatmap()` |\n| 压力重心、受力面积、最大值 | ✅ 有 | 每帧算好了，直接读 `frame.center` / `frame.area` / `frame.max` |\n| 换算成 kPa / 牛顿 | ❌ 没有 | **做不到**，见 §3 的警告 |\n| 曲线图 / 折线图 | ❌ 没有 | 你自己攒 `frame.max` 进数组，用 Chart.js / canvas 画 |\n| 录制与回放 | ❌ 没有 | 你自己把 `frame.raw` 存下来，回放时再 `decodeFrame()` |\n| 手势识别 / 姿态分类 | ❌ 没有 | 你自己在 `onFrame` 里写逻辑 |\n| 滤波、去噪 | ❌ 没有 | 你自己对 `frame.values` 处理（SDK 只在**显示**时做了柔化，数据是原始的） |\n\n---\n\n## 2. 三十秒上手\n\n浏览器（最常见）：\n\n```js\nimport { Shroom } from './sdk/web/index.js';\n\nconst heatmap = Shroom.createHeatmap('#view');       // #view 是一个 <canvas>\n\ndocument.querySelector('#btn').onclick = async () => {\n  const device = await Shroom.connect();             // ⚠️ 必须在点击回调里\n  device.onFrame((frame) => heatmap.render(frame));\n};\n```\n\nNode / Electron：\n\n```js\nimport { Shroom } from './sdk/node/index.js';        // 需要先 npm i serialport\n\nconst device = await Shroom.connect({ path: 'COM3' });\ndevice.onFrame((frame) => console.log(Shroom.renderAscii(frame)));\n```\n\n没有硬件时把 `Shroom.connect(...)` 换成 `Shroom.mock({ rows: 32, cols: 32, fps: 30 })`，\n**其余代码一个字都不用改**。写新功能时建议一律先用 mock 调通。\n\n> **`mock()` 是同步的**（`connect()` 是 async）。写成 `await Shroom.mock()` 也不会出错——\n> `await` 一个非 Promise 值就是它本身——所以两种写法都能跑。\n\n### mock 产生的是什么数据\n\n**两个高斯亮斑绕着中心转**（一大一小，反向旋转），外加一点底噪：\n\n- 峰值 `max` 稳定在 **0.9 以上**，底噪约 0.01\n- 亮斑**持续移动**，`center` 一直在变，绕中心画圈\n- 默认 32×32、30fps\n\n**它能验证什么**：渲染、`center` 跟随、`max`/`area` 读数、曲线、录制回放——链路整体是通的。\n\n**它不能验证什么**：\n- **真实的按压节奏**。mock 的 `max` 一直很高、从不「松手」，所以**「按下→抬起」这类状态机在 mock 下不会触发**。\n  测这类逻辑要么手动造数据，要么就得插真设备。\n- 你的传感器实际的数值范围和噪声水平。\n\n---\n\n## 3. Frame：唯一的数据结构\n\n不管数据从哪来（浏览器串口 / Node 串口 / 模拟），`onFrame` 拿到的都是这个：\n\n```ts\ninterface Frame {\n  raw: Uint8Array;        // 原始 ADC 值 0~255，一个点一个字节，长度 = 实际收到的点数\n  values: Float32Array;   // 归一化 0~1，长度 = rows * cols  ← 你几乎只用这个\n  rows: number;           // 矩阵行数\n  cols: number;           // 矩阵列数\n  min: number;            // values 的最小值\n  max: number;            // values 的最大值   ← 「按得多重」通常看它\n  avg: number;            // values 的平均值\n  area: number;           // 超过阈值(默认 0.02)的点数 ← 「接触面积有多大」看它\n  center: { x: number; y: number };  // 压力重心，x/y 都是 0~1，没受力时是 {0.5, 0.5}\n  timestamp: number;      // Date.now() 毫秒\n}\n```\n\n按行列取某个点：\n\n```js\nconst v = frame.values[y * frame.cols + x];   // 0~1\n```\n\n### 坐标系（做位置相关功能前必读）\n\n数据的排布方式是确定的，**不受任何显示选项影响**：\n\n```\nvalues 索引 = y * cols + x      x 是列（0 ~ cols-1），y 是行（0 ~ rows-1）\n\ncenter.x = 列方向，0 = 第 0 列，1 = 最后一列\ncenter.y = 行方向，0 = 第 0 行，1 = 最后一行\n```\n\n把 `center` 映射成 N×N 的格子（记得钳制，浮点可能取到边界）：\n\n```js\nconst col = Math.min(N - 1, Math.floor(frame.center.x * N));\nconst row = Math.min(N - 1, Math.floor(frame.center.y * N));\n```\n\n> **`flipY` 不影响 `center`。** `flipY` 只是 `'dots'` 模式**画图时**把行序颠倒一下，\n> `frame.values` 和 `frame.center` 永远是原始行序。所以你的位置判定逻辑不用管 `flipY`。\n>\n> **但传感器实物朝哪一边放，SDK 不知道。** 第 0 行是贴近用户的那边还是远离的那边，\n> 取决于设备怎么摆。**做位置类功能时给用户一个「上下翻转」开关**（`y → 1 - y`），\n> 比你猜一个方向可靠。\n\n> **多点按压时 `center` 是加权平均。** 两个手指分别按左上和右下，重心会落在正中间——\n> 那里可能根本没人按。SDK **没有**提供「最大压力点的坐标」，需要的话自己遍历 `values` 找最大值下标：\n>\n> ```js\n> let peak = 0, peakIdx = 0;\n> for (let i = 0; i < frame.values.length; i += 1) {\n>   if (frame.values[i] > peak) { peak = frame.values[i]; peakIdx = i; }\n> }\n> const px = peakIdx % frame.cols, py = Math.floor(peakIdx / frame.cols);\n> ```\n>\n> 单点触发类应用（按格子、打地鼠）用峰值点通常比用重心准。\n\n### 典型数值量级（写阈值时的依据）\n\n传感器逐片有差异，下面是**数量级参考**，不是标准值：\n\n| 状态 | `frame.max` 大致范围 |\n| --- | --- |\n| 没人碰 | 0.01 ~ 0.05（底噪） |\n| 手指轻触 | 0.1 ~ 0.3 |\n| 手指正常按 | 0.3 ~ 0.7 |\n| 用力按 / 手掌压 | 0.7 ~ 1.0 |\n\n所以「**判定为按下**」的阈值取 **0.15 左右**是个合理起点，`0.05` 只够用来判断「有没有人碰」。\n\n**更稳的做法是不写死阈值**，开头采一秒基线，然后按相对量触发：\n\n```js\n// 让用户别碰传感器，先量一下底噪\nlet baseline = 0.03;\nsetTimeout(() => { baseline = observedMax; }, 1000);\n\nconst pressed = frame.max > baseline + 0.12;\n```\n\n按压检测建议用**双阈值**，避免在临界点反复触发：\n\n```js\nif (!isDown && frame.max > 0.15) isDown = true;        // 按下\nelse if (isDown && frame.max < 0.08) isDown = false;   // 抬起（阈值更低）\n```\n\n> ### ⚠️ 关于单位：`values` 是相对值，不是物理量\n>\n> `values` 是 ADC 读数除以满量程得到的 **0~1 相对值**。它**不是** kPa、不是牛顿、不是克。\n>\n> 要换算成物理单位需要每一台设备各自的标定曲线（传感器是非线性的，且逐片有差异），\n> 这些数据**不在这个 SDK 里，也不会在**。\n>\n> 所以：\n> - ✅ 可以说「压力 0.72」「比刚才重了」「重心往左移了」\n> - ❌ **不要**在 UI 上写 kPa / N / kg，**不要**自己编一个系数去乘\n> - 如果用户明确要物理单位，告诉他需要找设备厂商拿标定数据，这是 SDK 之外的事\n\n`raw` 和 `values` 的长度可能不一致：`raw` 是实际收到的字节数，\n`values` 长度恒为 `rows * cols`，不够的位置补 0。日常用 `values` 就行。\n\n---\n\n## 4. 完整 API\n\n### 4.1 浏览器入口 `sdk/web/index.js`\n\n```js\nShroom.connect(options?)              // → Promise<Device>，必须在用户手势里调用\nShroom.mock(options?)                 // → Device，模拟数据\nShroom.createHeatmap(target, opts?)   // → Heatmap\nShroom.isSupported()                  // → boolean，当前浏览器能不能用串口\nShroom.version                        // '0.1.0'\n```\n\n也可以按名字导入，还额外导出了底层件（自己接别的数据源时用）：\n\n```js\nimport {\n  connectSerial, isSerialSupported, createHeatmap, createMockDevice,\n  createFramer, decodeFrame, resolveShape, DEFAULT_DELIMITER,\n  jet, jetWhite, grey, getColormap,\n} from './sdk/web/index.js';\n```\n\n### 4.2 Node 入口 `sdk/node/index.js`\n\n```js\nShroom.connect(options?)     // → Promise<Device>，options.path 不填就自动挑第一个串口\nShroom.listPorts()           // → Promise<PortInfo[]>，列出机器上的串口\nShroom.mock(options?)        // → Device\nShroom.renderAscii(frame, o?) // → string，终端里的彩色方块图，直接 console.log\n```\n\n需要 `npm i serialport`（原生模块，按需加载；不装也不影响 mock 和 core）。\n\n### 4.3 Device（连接后拿到的东西，两端一致）\n\n```js\ndevice.info            // { source: 'web-serial'|'node-serial'|'mock', rows, cols, baudRate|path|fps }\ndevice.onFrame(fn)     // 订阅每一帧，返回一个「取消订阅」的函数\nawait device.close()   // 断开\n\n// 下面四个只有真实串口设备有，mock 没有（用 ?. 或先判断 source）\ndevice.bytesReceived   // 收到的原始字节总数\ndevice.frameCount      // 成功切出的帧数\ndevice.droppedCount    // 被丢掉的脏帧数\ndevice.frameLength     // 锁定下来的帧长（字节），还没锁上是 0\n```\n\n`onFrame` 可以订阅多个；一个回调抛错不影响其他回调。取消订阅：\n\n```js\nconst off = device.onFrame(handler);\noff();   // 不再收\n```\n\n> **`close()` 会自动清掉所有订阅**，不用先挨个 `off()` 再 `close()`。\n> `off()` 只在「设备还连着，但某个模块不想再收了」时才需要。\n\n### 4.3.1 connect() 抛错的两种情况\n\n用户在串口选择框上点「取消」也会抛错，**要和真正的连接失败分开处理**，\n否则用户取消一下就看到一句「连接失败」，很莫名其妙：\n\n```js\ntry {\n  device = await Shroom.connect();\n} catch (err) {\n  // 用户主动取消：静默返回，什么都不用提示\n  if (err?.name === 'NotFoundError') return;\n  // 其余才是真的出问题了\n  showError('连接失败：' + err.message);\n}\n```\n\n### 4.3.2 切换设备时的顺序（容易踩）\n\n必须**先 connect，成功之后再关旧的**。反过来写会让第二次连接直接失败——\n因为 `await oldDevice.close()` 会消耗掉用户手势（见 §5.1）：\n\n```js\n// ✅ 对\nbutton.onclick = async () => {\n  const next = await Shroom.connect();   // 先连新的\n  await current?.close();                // 再关旧的\n  current = next;\n};\n\n// ❌ 错：第二次点就连不上了\nbutton.onclick = async () => {\n  await current?.close();                // ← 这一行让手势失效\n  current = await Shroom.connect();      // 抛错\n};\n```\n\n### 4.4 连接参数\n\n```js\nawait Shroom.connect({\n  baudRate: 1000000,                    // 默认 1000000\n  delimiter: [0xAA, 0x55, 0x03, 0x99],  // 帧分隔符，默认就是这个\n  rows: 32, cols: 32,                   // 不填按方阵推断：1024 个点 → 32×32\n  fullScale: 255,                       // 满量程，默认 255\n  threshold: 0.02,                      // 算 area / center 的有效点阈值\n  lockLength: true,                     // 锁定帧长，默认开，见 §6\n  filters: [{ usbVendorId: 0x1a86 }],   // 仅浏览器：过滤设备选择框\n  path: 'COM3',                         // 仅 Node：端口号\n});\n```\n\n### 4.5 Heatmap\n\n```js\nconst heatmap = Shroom.createHeatmap('#view', { mode: 'dots' });\nheatmap.render(frame);          // 记下这一帧，下个屏幕刷新周期画出来\nheatmap.setOptions({ gain: 2 }); // 运行中改任何选项\nheatmap.resize();               // 容器尺寸变了时调（一般不用手动调）\nheatmap.clear();                // 清空\nheatmap.canvas;                 // 拿到底层 <canvas>\n```\n\n选项：\n\n| 选项 | 默认 | 说明 |\n| --- | --- | --- |\n| `mode` | `'dots'` | `'dots'` 透视点阵 / `'heat'` 热力圆斑 / `'grid'` 方格 |\n| `colormap` | `'jet'` | `'jet'` / `'jetWhite'` / `'grey'`，或自定义函数 `(t) => [r,g,b]`，t 是 0~1 |\n| `gain` | `1` | 显示增益，信号弱时调大。**只影响显示，不改 `frame.values`** |\n| `dotSize` | `1` | 点 / 圆斑大小倍数 |\n| `smooth` | `true` | 是否平滑，只对 `'heat'` / `'grid'` 有效 |\n| `relief` | `1` | 点阵起伏高度倍数，给 `0` 就是平的俯视点阵。只对 `'dots'` 有效 |\n| `tilt` | `70` | 俯视角（度，5~89）。越大越像正俯视、方阵看着越方。只对 `'dots'` 有效 |\n| `flipY` | `true` | 数据第 0 行画在近处（画面下方）。**按下面却是上面鼓起来就设成 `false`**。只对 `'dots'` 有效 |\n\n`render()` 只是**记下**这一帧，实际绘制推到下一个屏幕刷新周期。所以哪怕串口一秒来 100 帧，\n也只会画 60 次，不用你自己做节流。\n\n> **canvas 尺寸交给 CSS 就行。** heatmap 内部按 `clientWidth/clientHeight × devicePixelRatio`\n> 自己设置画布分辨率，高分屏不会糊。你只要给 canvas 一个 CSS 尺寸：\n>\n> ```html\n> <canvas id=\"view\" style=\"width: 360px; height: 360px\"></canvas>\n> ```\n>\n> **不要**自己去写 `canvas.width = 360`，会被覆盖。容器尺寸变化一般能自动跟上，\n> 极端情况下手动调一次 `heatmap.resize()`。\n\n### 4.6 底层件（自己接数据源时用）\n\n想接 WebSocket、蓝牙、文件回放等 SDK 没提供的数据源，用这两个就能拿到同样的 Frame：\n\n```js\nimport { createFramer, decodeFrame } from './sdk/web/index.js';\n\nconst framer = createFramer();                    // 字节流 → 一帧一帧\nfor (const payload of framer.push(someBytes)) {   // push 返回本次切出的所有帧\n  const frame = decodeFrame(payload, { rows: 32, cols: 32 });\n  heatmap.render(frame);\n}\n```\n\n`createFramer()` 返回 `{ push(chunk), reset(), droppedCount, frameLength }`。\n\n---\n\n## 5. 硬性约束（违反了代码一定跑不起来）\n\n### 5.1 浏览器串口必须在用户手势里调用\n\n```js\n// ✅ 对\nbutton.onclick = async () => { const d = await Shroom.connect(); };\n\n// ❌ 错：页面一加载就连，浏览器直接拒绝\nwindow.onload = async () => { const d = await Shroom.connect(); };\n\n// ❌ 错：await 了别的东西之后再 connect，用户手势已经过期\nbutton.onclick = async () => {\n  await fetch('/api/config');       // ← 这一行让手势失效\n  const d = await Shroom.connect(); // 抛错\n};\n```\n\n要先取配置就**先 connect 再 fetch**，或者把配置提前准备好。\n\n### 5.2 浏览器串口只在 https 或 localhost 下开放\n\n`navigator.serial` 标了 `SecureContext`。所以：\n\n- `http://localhost:xxxx` ✅\n- `https://你的域名` ✅\n- `file://`（双击 html 文件打开）❌ **永远不行**，任何打包方式都绕不过\n- `http://192.168.x.x` ❌\n\n写代码时用 `Shroom.isSupported()` 提前判断，给用户一个明确的提示，别让按钮点了没反应。\n\n### 5.3 只有 Chrome / Edge 支持\n\nSafari 和 Firefox 没有 Web Serial，且短期内不会有。需要跨浏览器就走 Node/Electron 方案。\n\n**操作系统不是限制**：Windows / macOS / Linux 三个桌面系统上的 Chrome / Edge 都能用，\n代码一份，不用写平台分支。移动端不行（Android Chrome 也没有 Web Serial）。\n\n写给用户看的提示文案里，**别写「请在 Windows 上运行」**——限制是浏览器，不是系统。\n\n### 5.4 帧率很高，回调里别做重活\n\n一秒可能来 100 帧。`onFrame` 里做 DOM 操作、`JSON.stringify`、\n`console.log` 大对象都会卡。要更新 UI 文字就自己降频：\n\n```js\nlet last = 0;\ndevice.onFrame((frame) => {\n  heatmap.render(frame);                    // 这个内部已经按屏幕刷新率节流了\n  if (frame.timestamp - last > 100) {       // 文字每 100ms 更新一次就够了\n    last = frame.timestamp;\n    label.textContent = frame.max.toFixed(3);\n  }\n});\n```\n\n### 5.5 别持有 frame 对象\n\n`frame.raw` 是原始 buffer 的视图，`frame.values` 每帧都是新的。要存起来做回放/曲线，\n**存副本或存标量**，别把整个 frame 塞进数组：\n\n```js\n// ✅ 存标量\nhistory.push(frame.max);\n\n// ✅ 存副本\nrecorded.push(new Uint8Array(frame.raw));\n\n// ❌ 存引用，内存会炸，raw 还可能被复用\nrecorded.push(frame);\n```\n\n---\n\n## 6. 帧长锁定（`lockLength`，默认开）\n\n分隔符只有 4 字节，数据里迟早会撞出一串一模一样的，于是切出长度不对的脏帧。\n切帧器默认会锁定帧长把这些丢掉，也能自动纠回来。\n\n**你只需要知道一件事：`droppedCount` 有个小数字在缓慢增长是正常的，不用报错给用户。**\n\n（真需要收变长帧就传 `lockLength: false`。）\n\n---\n\n## 7. 排错对照表\n\n用户说「连上了但没画面」，让他看这三个数：\n\n| 现象 | 原因 | 怎么办 |\n| --- | --- | --- |\n| `bytesReceived === 0` | 设备根本没在发数据 | 选错 COM 口 / 线坏了 / 设备要先发启动指令 |\n| 字节在涨但 `frameCount === 0` | 波特率或分隔符不对 | 依次试 `1000000`（默认）、`921600`、`1500000`、`3000000`、`115200` |\n| 帧在涨但画面是一条横线 | 帧长不是完全平方数，推不出方阵 | 显式传 `rows` / `cols`；`device.frameLength` 就是实际帧长 |\n| 帧在涨但画面花 | 矩阵尺寸猜错了 | 同上，显式传 `rows` / `cols` |\n| `droppedCount` 缓慢增长 | 假分隔符被帧长锁定挡掉了 | 正常，不用管 |\n| `droppedCount` 猛涨 | 波特率不稳 | 换一档波特率 |\n| 点阵按下面却是上面鼓起来 | 传感器行序和默认相反 | `heatmap.setOptions({ flipY: false })` |\n| 连接按钮点了没反应 | 不在用户手势里 / 不是安全上下文 | 见 §5.1、§5.2 |\n| 浏览器弹了选择框但列表是空的 | 设备没插好，或系统缺驱动 / 缺权限 | 见下面这行 |\n| Node 端 `Permission denied` 或 `listPorts()` 返回空 | Linux 上普通用户默认没有串口权限 | `sudo usermod -aG dialout $USER` 后重新登录；设备名一般是 `/dev/ttyUSB0` |\n\n---\n\n## 8. 可以直接抄的例子\n\n### 8.1 用重心控制一个小球\n\n```js\nconst heatmap = Shroom.createHeatmap('#view');\nconst ball = document.querySelector('#ball');\n\ndevice.onFrame((frame) => {\n  heatmap.render(frame);\n  if (frame.max < 0.05) return;                   // 没人按就别动\n  ball.style.left = frame.center.x * 100 + '%';\n  ball.style.top  = frame.center.y * 100 + '%';\n  ball.style.background = frame.max > 0.5 ? 'red' : 'white';\n});\n```\n\n### 8.2 按到哪个格子（打地鼠这类）\n\n把传感器分成 N×N 格，检测「按下 → 抬起」并判定落在哪一格：\n\n```js\nconst N = 3;\nlet isDown = false, peakMax = 0, peakCenter = null;\n\ndevice.onFrame((frame) => {\n  heatmap.render(frame);\n\n  if (!isDown && frame.max > 0.15) {           // 按下\n    isDown = true; peakMax = 0;\n  }\n  if (isDown) {\n    if (frame.max > peakMax) {                 // 记住最重那一刻的位置\n      peakMax = frame.max;\n      peakCenter = { x: frame.center.x, y: frame.center.y };   // 存副本\n    }\n    if (frame.max < 0.08) {                    // 抬起，结算\n      isDown = false;\n      const col = Math.min(N - 1, Math.floor(peakCenter.x * N));\n      const row = Math.min(N - 1, Math.floor(peakCenter.y * N));\n      onHit(row, col);                         // ← 你的逻辑\n    }\n  }\n});\n```\n\n两个要点：**取按压过程中最重那一帧的位置**（刚碰到和快松手时重心会飘），\n以及**双阈值**（0.15 按下 / 0.08 抬起）避免在临界点反复触发。\n\n> mock 数据 `max` 一直很高、不会「松手」，**这段逻辑在 mock 下不会触发**，需要真设备测。\n\n### 8.3 最大压力曲线\n\n```js\nconst history = [];\ndevice.onFrame((frame) => {\n  history.push(frame.max);                        // 存标量，不是整个 frame\n  if (history.length > 300) history.shift();      // 只留最近 300 点\n});\n\nsetInterval(() => drawCurve(history), 50);        // 画图跟采集分开，别在 onFrame 里画\n```\n\n### 8.4 录制与回放\n\n```js\nimport { decodeFrame } from './sdk/web/index.js';\n\nconst tape = [];\nconst off = device.onFrame((f) => tape.push(new Uint8Array(f.raw)));  // 存副本\n\n// 停止录制\noff();\n\n// 回放\nlet i = 0;\nconst timer = setInterval(() => {\n  if (i >= tape.length) return clearInterval(timer);\n  heatmap.render(decodeFrame(tape[i++], { rows: 32, cols: 32 }));\n}, 33);\n```\n\n### 8.5 分区域统计（比如把 32×32 分成四象限）\n\n```js\nfunction quadrantSums(frame) {\n  const { values, rows, cols } = frame;\n  const q = [0, 0, 0, 0];\n  for (let y = 0; y < rows; y += 1) {\n    for (let x = 0; x < cols; x += 1) {\n      const idx = (y < rows / 2 ? 0 : 2) + (x < cols / 2 ? 0 : 1);\n      q[idx] += values[y * cols + x];\n    }\n  }\n  return q;    // [左上, 右上, 左下, 右下]\n}\n```\n\n### 8.6 简单去噪（SDK 不提供，自己两行）\n\n```js\nlet smoothed = null;\ndevice.onFrame((frame) => {\n  if (!smoothed || smoothed.length !== frame.values.length) {\n    smoothed = new Float32Array(frame.values);\n  }\n  for (let i = 0; i < smoothed.length; i += 1) {\n    smoothed[i] += (frame.values[i] - smoothed[i]) * 0.3;   // 越小越黏\n  }\n  // 拿 smoothed 去做你的判断\n});\n```\n\n---\n\n## 9. 目录结构（主要用来确认 import 路径）\n\n```\nsdk/\n├─ core/              纯逻辑，浏览器和 Node 共用\n│  ├─ framer.js         字节流 → 一帧一帧\n│  ├─ frame.js          一帧字节 → Frame 对象\n│  ├─ colormap.js       0~1 → [r,g,b]\n│  ├─ device.js         帧订阅中心\n│  └─ mock.js           模拟数据源\n├─ web/               浏览器\n│  ├─ index.js          ← 浏览器入口，import 这个\n│  ├─ serial.js         Web Serial 连接\n│  ├─ heatmap.js        canvas 渲染\n│  ├─ index.html        官方示例页\n│  └─ shroom.bundle.js  单文件版，<script> 直接引\n├─ node/               Node / Electron\n│  └─ index.js          ← Node 入口，import 这个\n├─ start.mjs          本地服务器（根目录 = sdk/，端口 5178 起，占用则顺延到 5182）\n└─ index.d.ts         TypeScript 类型定义\n```\n\n**你只需要 import 两个入口之一**：`web/index.js` 或 `node/index.js`，\n路径怎么写见 §0.5。其余文件不用读，本文已经涵盖了它们的全部对外行为。\n\n> 唯一的例外是 §4.6 的 `createFramer` / `decodeFrame`——接非串口数据源时才用，\n> 它们同样从入口文件导出，不用去 `core/` 里单独引。\n\n---\n\n## 10. 写代码时的默认选择\n\n除非用户另有要求，按这个来：\n\n- **先用 `Shroom.mock()` 把功能跑通**，最后再换成 `Shroom.connect()`。用户往往手边没设备。\n- 浏览器方案优先，除非用户明确说要 Electron / 后端 / 命令行。\n- 连接按钮的文案和禁用状态要处理好：先 `Shroom.isSupported()` 判断，\n  不支持就禁用按钮并说明原因（是 `file://`？还是浏览器不对？），别让用户点了没反应。\n- 数值一律按 0~1 显示，或者显示成百分比，**不要编造物理单位**。\n- `onFrame` 里只做计算和 `heatmap.render()`，DOM 更新降频到 10Hz 左右。\n- 需要本地起服务时提醒用户：不能双击 html 文件，要 `node start.mjs`（SDK 里自带）。\n",
    "toc": [
      {
        "id": "s1",
        "text": "0. 一句话",
        "level": 2
      },
      {
        "id": "s2",
        "text": "0.5 先确定 import 路径（第一行代码就会错）",
        "level": 2
      },
      {
        "id": "s3",
        "text": "1. 能力边界（先读这段，能省掉一半错误）",
        "level": 2
      },
      {
        "id": "s4",
        "text": "2. 三十秒上手",
        "level": 2
      },
      {
        "id": "s5",
        "text": "mock 产生的是什么数据",
        "level": 3
      },
      {
        "id": "s6",
        "text": "3. Frame：唯一的数据结构",
        "level": 2
      },
      {
        "id": "s7",
        "text": "坐标系（做位置相关功能前必读）",
        "level": 3
      },
      {
        "id": "s8",
        "text": "典型数值量级（写阈值时的依据）",
        "level": 3
      },
      {
        "id": "s9",
        "text": "⚠️ 关于单位：values 是相对值，不是物理量",
        "level": 3
      },
      {
        "id": "s10",
        "text": "4. 完整 API",
        "level": 2
      },
      {
        "id": "s11",
        "text": "4.1 浏览器入口 sdk/web/index.js",
        "level": 3
      },
      {
        "id": "s12",
        "text": "4.2 Node 入口 sdk/node/index.js",
        "level": 3
      },
      {
        "id": "s13",
        "text": "4.3 Device（连接后拿到的东西，两端一致）",
        "level": 3
      },
      {
        "id": "s14",
        "text": "4.3.1 connect() 抛错的两种情况",
        "level": 3
      },
      {
        "id": "s15",
        "text": "4.3.2 切换设备时的顺序（容易踩）",
        "level": 3
      },
      {
        "id": "s16",
        "text": "4.4 连接参数",
        "level": 3
      },
      {
        "id": "s17",
        "text": "4.5 Heatmap",
        "level": 3
      },
      {
        "id": "s18",
        "text": "4.6 底层件（自己接数据源时用）",
        "level": 3
      },
      {
        "id": "s19",
        "text": "5. 硬性约束（违反了代码一定跑不起来）",
        "level": 2
      },
      {
        "id": "s20",
        "text": "5.1 浏览器串口必须在用户手势里调用",
        "level": 3
      },
      {
        "id": "s21",
        "text": "5.2 浏览器串口只在 https 或 localhost 下开放",
        "level": 3
      },
      {
        "id": "s22",
        "text": "5.3 只有 Chrome / Edge 支持",
        "level": 3
      },
      {
        "id": "s23",
        "text": "5.4 帧率很高，回调里别做重活",
        "level": 3
      },
      {
        "id": "s24",
        "text": "5.5 别持有 frame 对象",
        "level": 3
      },
      {
        "id": "s25",
        "text": "6. 帧长锁定（lockLength，默认开）",
        "level": 2
      },
      {
        "id": "s26",
        "text": "7. 排错对照表",
        "level": 2
      },
      {
        "id": "s27",
        "text": "8. 可以直接抄的例子",
        "level": 2
      },
      {
        "id": "s28",
        "text": "8.1 用重心控制一个小球",
        "level": 3
      },
      {
        "id": "s29",
        "text": "8.2 按到哪个格子（打地鼠这类）",
        "level": 3
      },
      {
        "id": "s30",
        "text": "8.3 最大压力曲线",
        "level": 3
      },
      {
        "id": "s31",
        "text": "8.4 录制与回放",
        "level": 3
      },
      {
        "id": "s32",
        "text": "8.5 分区域统计（比如把 32×32 分成四象限）",
        "level": 3
      },
      {
        "id": "s33",
        "text": "8.6 简单去噪（SDK 不提供，自己两行）",
        "level": 3
      },
      {
        "id": "s34",
        "text": "9. 目录结构（主要用来确认 import 路径）",
        "level": 2
      },
      {
        "id": "s35",
        "text": "10. 写代码时的默认选择",
        "level": 2
      }
    ]
  },
  {
    "slug": "ai-guide",
    "label": "怎么用 AI 来开发",
    "summary": "写给不写代码的人：怎么把 SDK 交给 AI，怎么描述需求，出问题了怎么跟它说。",
    "forAi": false,
    "file": "怎么用AI开发.md",
    "title": "怎么用 AI 来开发",
    "html": "<p>你手上这个 SDK，只负责一件事：<strong>把压力传感器的数据稳定地送到你手上，并且画出来。</strong></p>\n<p>至于做成什么——游戏、康复训练、坐姿提醒、压力曲线——那部分不在 SDK 里，\n但你不需要自己写，让 AI 写就行。这篇讲的就是怎么让 AI 写对。</p>\n<p>不需要会编程。但<strong>要能照着步骤操作</strong>，比如解压文件、双击运行、复制粘贴。</p>\n<hr>\n<h2 id=\"s1\">一、先确认设备是好的（5 分钟，别跳过）</h2>\n<p><strong>先别急着找 AI。</strong> 万一后面出问题，你得能分清是「AI 代码写错了」还是「设备本来就没连上」。\n先跑通官方示例，后面就有了一个确定的参照。</p>\n<ol>\n<li><p>解压 SDK，按你的系统启动：</p>\n<table>\n<thead>\n<tr>\n<th>系统</th>\n<th>怎么启动</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Windows</td>\n<td>双击 <strong><code>start-demo.bat</code></strong></td>\n</tr>\n<tr>\n<td>macOS / Linux</td>\n<td>打开「终端」，<code>cd</code> 到解压出来的文件夹，敲 <strong><code>sh start.sh</code></strong></td>\n</tr>\n</tbody></table>\n<blockquote>\n<p>macOS 上<strong>双击 <code>.sh</code> 文件是没用的</strong>，系统会用文本编辑器打开它，不会运行。\n必须在终端里敲命令。</p>\n</blockquote>\n</li>\n<li><p>会自动弹出浏览器（<strong>必须是 Chrome 或 Edge</strong>）</p>\n</li>\n<li><p>插上设备，点「<strong>连接设备</strong>」，在弹出的列表里选中你的设备</p>\n</li>\n<li><p>应该看到一片点阵，用手按上去会鼓起来、变颜色</p>\n</li>\n</ol>\n<p>看到了就说明设备、驱动、浏览器全都正常，可以进下一步。</p>\n<p>看不到？先点「<strong>用模拟数据</strong>」试试：</p>\n<ul>\n<li><strong>模拟数据能动</strong> → 设备或驱动的问题，看下面这张表</li>\n<li><strong>模拟数据也不动</strong> → 浏览器不对（Safari / Firefox 不支持），换 Chrome</li>\n</ul>\n<p>设备连不上时，先按系统对照一下：</p>\n<table>\n<thead>\n<tr>\n<th>系统</th>\n<th>最常见的原因</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Windows</td>\n<td>没装 USB 串口驱动。装 CH341SER，然后在设备管理器里确认出现了 COM 口</td>\n</tr>\n<tr>\n<td>macOS</td>\n<td>较新的系统自带驱动，插上后设备名形如 <code>/dev/tty.usbserial-*</code>；没有就装一下芯片厂商的驱动</td>\n</tr>\n<tr>\n<td>Linux</td>\n<td>驱动通常自带，但<strong>当前用户不在 <code>dialout</code> 组</strong>，打开串口会 Permission denied。执行 <code>sudo usermod -aG dialout $USER</code>，然后重新登录</td>\n</tr>\n</tbody></table>\n<p>还不行就翻 <a href=\"README.md\">README.md</a> 的完整排错表。</p>\n<blockquote>\n<p><strong>没有设备也能全程往下走。</strong> 「用模拟数据」会生成假数据，\n写代码、调界面、给 AI 测试都够用，等设备到了改一行就能换成真的。</p>\n</blockquote>\n<hr>\n<h2 id=\"s2\">二、把说明书交给 AI</h2>\n<p>SDK 里有一份专门写给 AI 读的文件：</p>\n<pre><code>AI-CONTEXT.md\n</code></pre>\n<p><strong>把这个文件整个发给 AI。</strong> 按你用的工具选一种：</p>\n<table>\n<thead>\n<tr>\n<th>你用的</th>\n<th>怎么做</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>ChatGPT / Claude 网页版</td>\n<td>把文件直接拖进对话框</td>\n</tr>\n<tr>\n<td>Claude Code / Cursor / Copilot</td>\n<td>把 SDK 文件夹放进项目，说「先读一下 <code>sdk/AI-CONTEXT.md</code>」</td>\n</tr>\n<tr>\n<td>只能粘文字的</td>\n<td>用记事本打开，Ctrl+A 全选，Ctrl+C 复制，粘进去</td>\n</tr>\n</tbody></table>\n<p><strong>这一步不能省。</strong> 不给这个文件，AI 会凭空猜函数名字——写出来的代码看着挺像回事，\n一跑就报错，然后你和它会在错误的方向上来回折腾很久。</p>\n<blockquote>\n<p>换了新对话、或者聊太久 AI 开始犯迷糊时，<strong>把文件重新发一次</strong>。\nAI 记不住之前对话里的东西。</p>\n</blockquote>\n<hr>\n<h2 id=\"s3\">三、说清楚你要什么</h2>\n<p>发完文件，用大白话描述就行：</p>\n<blockquote>\n<p>我有一个压力传感器 SDK，说明在我刚发给你的文件里。\n帮我做一个网页：屏幕上有个小球，我按传感器的哪个位置，小球就移动到哪，\n按得越重球越大。</p>\n</blockquote>\n<p><strong>说清这四件事，返工会少很多：</strong></p>\n<ol>\n<li><strong>要看到什么</strong> —— 一个页面？一个数字？一条曲线？一个游戏？</li>\n<li><strong>什么条件触发什么</strong> —— 按到多重算数？持续多久？</li>\n<li><strong>在哪跑</strong> —— 网页就行，还是要打包成能双击的桌面软件？</li>\n<li><strong>有没有设备</strong> —— 没有的话加一句「<strong>先用模拟数据</strong>」，AI 就会写成不插设备也能调的版本</li>\n</ol>\n<p>最后可以补一句，能省掉一轮返工：</p>\n<blockquote>\n<p>请写成单个 HTML 文件，我放进 sdk/web/ 目录里就能直接打开。</p>\n</blockquote>\n<hr>\n<h2 id=\"s4\">四、常见需求，直接抄</h2>\n<p>改改细节发给 AI 就能用：</p>\n<p><strong>看数据</strong></p>\n<blockquote>\n<p>做一个页面，左边热力图，右边实时显示最大压力、受力面积、重心坐标三个数字。</p>\n</blockquote>\n<p><strong>画曲线</strong></p>\n<blockquote>\n<p>热力图下面加一条曲线，显示最近 30 秒的最大压力变化。</p>\n</blockquote>\n<p><strong>做判断</strong></p>\n<blockquote>\n<p>当压力超过 0.6 并持续 2 秒，页面变红并播放提示音。</p>\n</blockquote>\n<p><strong>分区统计</strong></p>\n<blockquote>\n<p>把传感器分成左右两半，分别显示两边的压力总和，用两根柱子表示。</p>\n</blockquote>\n<p><strong>录制回放</strong></p>\n<blockquote>\n<p>加录制和回放按钮，录制时数据存在内存里，回放时按原速播放。</p>\n</blockquote>\n<p><strong>小游戏</strong></p>\n<blockquote>\n<p>做一个打地鼠：随机位置出现一个圆点，我按中那个位置就得一分。</p>\n</blockquote>\n<p><strong>坐姿提醒</strong></p>\n<blockquote>\n<p>显示热力图，如果重心偏左或偏右超过 20% 并持续 10 秒，弹提示让我坐正。</p>\n</blockquote>\n<hr>\n<h2 id=\"s5\">五、AI 写好的代码怎么跑起来</h2>\n<p>AI 一般会给你一个 <code>.html</code> 文件。</p>\n<blockquote>\n<h3 id=\"s6\">⚠️ 不能双击打开 HTML 文件</h3>\n<p>浏览器规定：<strong>串口只在 <code>https</code> 或 <code>localhost</code> 下才能用</strong>。\n双击打开文件时地址栏是 <code>file://</code>，串口功能直接不存在——\n这是浏览器的硬规定，<strong>没有任何办法绕过</strong>，AI 也绕不过。</p>\n<p>症状很好认：页面能打开、按钮能点，但点「连接设备」毫无反应。</p>\n</blockquote>\n<p><strong>正确做法：</strong></p>\n<ol>\n<li>把 AI 给你的文件放进 <strong><code>sdk/web/</code></strong> 文件夹里</li>\n<li>照常双击 <code>start-demo.bat</code></li>\n<li>浏览器打开后，把地址栏末尾的 <code>index.html</code> 改成你的文件名</li>\n</ol>\n<p>比如 AI 给了 <code>game.html</code>，放进 <code>sdk/web/</code>，然后访问：</p>\n<pre><code>http://localhost:5178/game.html\n</code></pre>\n<blockquote>\n<p>如果 5178 被别的程序占用了，启动时会自动往后挑 5179、5180……\n<strong>以黑窗口里实际打印的地址为准</strong>，别死记 5178。</p>\n</blockquote>\n<hr>\n<h2 id=\"s7\">六、出问题了怎么跟 AI 说</h2>\n<p>代码一次跑通是少数情况，来回两三轮很正常。关键是<strong>把具体现象告诉它</strong>，别只说「不行」。</p>\n<table>\n<thead>\n<tr>\n<th>情况</th>\n<th>怎么跟 AI 说</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>页面白屏</td>\n<td>按 <strong>F12</strong> 打开 Console，把<strong>红色报错整段</strong>复制给它</td>\n</tr>\n<tr>\n<td>按钮点了没反应</td>\n<td>同上，先看 Console 有没有报错</td>\n</tr>\n<tr>\n<td>有画面但数据不对</td>\n<td>说清「我期望看到什么，实际看到什么」</td>\n</tr>\n<tr>\n<td>连不上设备</td>\n<td>先用官方示例页确认设备本身是好的，再说</td>\n</tr>\n<tr>\n<td>画面在方阵和一条横线之间闪</td>\n<td>告诉 AI「需要显式指定 rows 和 cols」</td>\n</tr>\n</tbody></table>\n<p>万能句式：</p>\n<blockquote>\n<p>报错是：（原样贴上报错）\nSDK 说明在我前面发的 AI-CONTEXT.md 里，请对照着看是哪里用错了。</p>\n</blockquote>\n<p><strong>AI 改了三四轮还是不行</strong>，别继续绕。开一个新对话，重新发一遍 AI-CONTEXT.md，\n把需求说得更具体些——通常比在旧对话里死磕快。</p>\n<hr>\n<h2 id=\"s8\">七、几件事先说明白，免得白折腾</h2>\n<p><strong>数据是 0~1 的相对值，不是 kPa、不是公斤。</strong>\n这是最容易误会的一点。SDK 给的是传感器原始读数归一化后的值，\n要换算成真实物理单位，需要每台设备各自的标定数据，那不在这个 SDK 里。</p>\n<p><strong>如果 AI 给你写出了「12.5 kPa」这种，那是它编的，数字不准。</strong>\n需要真实单位得找设备厂商要标定数据。</p>\n<p><strong>只有 Chrome 和 Edge 能连串口。</strong> Safari 和 Firefox 没这功能，短期内也不会有。</p>\n<p><strong>SDK 里没有曲线、回放、算法这些。</strong> 它只管连接、取数、显示三件事。\n其余都要 AI 现写——这是有意设计成这样的，不是缺功能。</p>\n<p><strong>改坏了不要紧。</strong> 你自己写的东西单独放一个文件夹，别和 SDK 原有文件混在一起。\n真弄乱了，重新解压一份 SDK 就行。</p>\n<hr>\n<h2 id=\"s9\">八、想深入了解</h2>\n<ul>\n<li><a href=\"README.md\">README.md</a> —— SDK 完整说明和排错表</li>\n<li><a href=\"AI-CONTEXT.md\">AI-CONTEXT.md</a> —— 给 AI 看的那份，你自己想看也行，写得挺全</li>\n<li><a href=\"index.d.ts\">index.d.ts</a> —— TypeScript 类型定义，用 TS 时 AI 会需要</li>\n</ul>\n",
    "raw": "# 怎么用 AI 来开发\n\n你手上这个 SDK，只负责一件事：**把压力传感器的数据稳定地送到你手上，并且画出来。**\n\n至于做成什么——游戏、康复训练、坐姿提醒、压力曲线——那部分不在 SDK 里，\n但你不需要自己写，让 AI 写就行。这篇讲的就是怎么让 AI 写对。\n\n不需要会编程。但**要能照着步骤操作**，比如解压文件、双击运行、复制粘贴。\n\n---\n\n## 一、先确认设备是好的（5 分钟，别跳过）\n\n**先别急着找 AI。** 万一后面出问题，你得能分清是「AI 代码写错了」还是「设备本来就没连上」。\n先跑通官方示例，后面就有了一个确定的参照。\n\n1. 解压 SDK，按你的系统启动：\n\n   | 系统 | 怎么启动 |\n   | --- | --- |\n   | Windows | 双击 **`start-demo.bat`** |\n   | macOS / Linux | 打开「终端」，`cd` 到解压出来的文件夹，敲 **`sh start.sh`** |\n\n   > macOS 上**双击 `.sh` 文件是没用的**，系统会用文本编辑器打开它，不会运行。\n   > 必须在终端里敲命令。\n\n2. 会自动弹出浏览器（**必须是 Chrome 或 Edge**）\n3. 插上设备，点「**连接设备**」，在弹出的列表里选中你的设备\n4. 应该看到一片点阵，用手按上去会鼓起来、变颜色\n\n看到了就说明设备、驱动、浏览器全都正常，可以进下一步。\n\n看不到？先点「**用模拟数据**」试试：\n\n- **模拟数据能动** → 设备或驱动的问题，看下面这张表\n- **模拟数据也不动** → 浏览器不对（Safari / Firefox 不支持），换 Chrome\n\n设备连不上时，先按系统对照一下：\n\n| 系统 | 最常见的原因 |\n| --- | --- |\n| Windows | 没装 USB 串口驱动。装 CH341SER，然后在设备管理器里确认出现了 COM 口 |\n| macOS | 较新的系统自带驱动，插上后设备名形如 `/dev/tty.usbserial-*`；没有就装一下芯片厂商的驱动 |\n| Linux | 驱动通常自带，但**当前用户不在 `dialout` 组**，打开串口会 Permission denied。执行 `sudo usermod -aG dialout $USER`，然后重新登录 |\n\n还不行就翻 [README.md](README.md) 的完整排错表。\n\n> **没有设备也能全程往下走。** 「用模拟数据」会生成假数据，\n> 写代码、调界面、给 AI 测试都够用，等设备到了改一行就能换成真的。\n\n---\n\n## 二、把说明书交给 AI\n\nSDK 里有一份专门写给 AI 读的文件：\n\n```\nAI-CONTEXT.md\n```\n\n**把这个文件整个发给 AI。** 按你用的工具选一种：\n\n| 你用的 | 怎么做 |\n| --- | --- |\n| ChatGPT / Claude 网页版 | 把文件直接拖进对话框 |\n| Claude Code / Cursor / Copilot | 把 SDK 文件夹放进项目，说「先读一下 `sdk/AI-CONTEXT.md`」 |\n| 只能粘文字的 | 用记事本打开，Ctrl+A 全选，Ctrl+C 复制，粘进去 |\n\n**这一步不能省。** 不给这个文件，AI 会凭空猜函数名字——写出来的代码看着挺像回事，\n一跑就报错，然后你和它会在错误的方向上来回折腾很久。\n\n> 换了新对话、或者聊太久 AI 开始犯迷糊时，**把文件重新发一次**。\n> AI 记不住之前对话里的东西。\n\n---\n\n## 三、说清楚你要什么\n\n发完文件，用大白话描述就行：\n\n> 我有一个压力传感器 SDK，说明在我刚发给你的文件里。\n> 帮我做一个网页：屏幕上有个小球，我按传感器的哪个位置，小球就移动到哪，\n> 按得越重球越大。\n\n**说清这四件事，返工会少很多：**\n\n1. **要看到什么** —— 一个页面？一个数字？一条曲线？一个游戏？\n2. **什么条件触发什么** —— 按到多重算数？持续多久？\n3. **在哪跑** —— 网页就行，还是要打包成能双击的桌面软件？\n4. **有没有设备** —— 没有的话加一句「**先用模拟数据**」，AI 就会写成不插设备也能调的版本\n\n最后可以补一句，能省掉一轮返工：\n\n> 请写成单个 HTML 文件，我放进 sdk/web/ 目录里就能直接打开。\n\n---\n\n## 四、常见需求，直接抄\n\n改改细节发给 AI 就能用：\n\n**看数据**\n> 做一个页面，左边热力图，右边实时显示最大压力、受力面积、重心坐标三个数字。\n\n**画曲线**\n> 热力图下面加一条曲线，显示最近 30 秒的最大压力变化。\n\n**做判断**\n> 当压力超过 0.6 并持续 2 秒，页面变红并播放提示音。\n\n**分区统计**\n> 把传感器分成左右两半，分别显示两边的压力总和，用两根柱子表示。\n\n**录制回放**\n> 加录制和回放按钮，录制时数据存在内存里，回放时按原速播放。\n\n**小游戏**\n> 做一个打地鼠：随机位置出现一个圆点，我按中那个位置就得一分。\n\n**坐姿提醒**\n> 显示热力图，如果重心偏左或偏右超过 20% 并持续 10 秒，弹提示让我坐正。\n\n---\n\n## 五、AI 写好的代码怎么跑起来\n\nAI 一般会给你一个 `.html` 文件。\n\n> ### ⚠️ 不能双击打开 HTML 文件\n>\n> 浏览器规定：**串口只在 `https` 或 `localhost` 下才能用**。\n> 双击打开文件时地址栏是 `file://`，串口功能直接不存在——\n> 这是浏览器的硬规定，**没有任何办法绕过**，AI 也绕不过。\n>\n> 症状很好认：页面能打开、按钮能点，但点「连接设备」毫无反应。\n\n**正确做法：**\n\n1. 把 AI 给你的文件放进 **`sdk/web/`** 文件夹里\n2. 照常双击 `start-demo.bat`\n3. 浏览器打开后，把地址栏末尾的 `index.html` 改成你的文件名\n\n比如 AI 给了 `game.html`，放进 `sdk/web/`，然后访问：\n\n```\nhttp://localhost:5178/game.html\n```\n\n> 如果 5178 被别的程序占用了，启动时会自动往后挑 5179、5180……\n> **以黑窗口里实际打印的地址为准**，别死记 5178。\n\n---\n\n## 六、出问题了怎么跟 AI 说\n\n代码一次跑通是少数情况，来回两三轮很正常。关键是**把具体现象告诉它**，别只说「不行」。\n\n| 情况 | 怎么跟 AI 说 |\n| --- | --- |\n| 页面白屏 | 按 **F12** 打开 Console，把**红色报错整段**复制给它 |\n| 按钮点了没反应 | 同上，先看 Console 有没有报错 |\n| 有画面但数据不对 | 说清「我期望看到什么，实际看到什么」 |\n| 连不上设备 | 先用官方示例页确认设备本身是好的，再说 |\n| 画面在方阵和一条横线之间闪 | 告诉 AI「需要显式指定 rows 和 cols」 |\n\n万能句式：\n\n> 报错是：（原样贴上报错）\n> SDK 说明在我前面发的 AI-CONTEXT.md 里，请对照着看是哪里用错了。\n\n**AI 改了三四轮还是不行**，别继续绕。开一个新对话，重新发一遍 AI-CONTEXT.md，\n把需求说得更具体些——通常比在旧对话里死磕快。\n\n---\n\n## 七、几件事先说明白，免得白折腾\n\n**数据是 0~1 的相对值，不是 kPa、不是公斤。**\n这是最容易误会的一点。SDK 给的是传感器原始读数归一化后的值，\n要换算成真实物理单位，需要每台设备各自的标定数据，那不在这个 SDK 里。\n\n**如果 AI 给你写出了「12.5 kPa」这种，那是它编的，数字不准。**\n需要真实单位得找设备厂商要标定数据。\n\n**只有 Chrome 和 Edge 能连串口。** Safari 和 Firefox 没这功能，短期内也不会有。\n\n**SDK 里没有曲线、回放、算法这些。** 它只管连接、取数、显示三件事。\n其余都要 AI 现写——这是有意设计成这样的，不是缺功能。\n\n**改坏了不要紧。** 你自己写的东西单独放一个文件夹，别和 SDK 原有文件混在一起。\n真弄乱了，重新解压一份 SDK 就行。\n\n---\n\n## 八、想深入了解\n\n- [README.md](README.md) —— SDK 完整说明和排错表\n- [AI-CONTEXT.md](AI-CONTEXT.md) —— 给 AI 看的那份，你自己想看也行，写得挺全\n- [index.d.ts](index.d.ts) —— TypeScript 类型定义，用 TS 时 AI 会需要\n",
    "toc": [
      {
        "id": "s1",
        "text": "一、先确认设备是好的（5 分钟，别跳过）",
        "level": 2
      },
      {
        "id": "s2",
        "text": "二、把说明书交给 AI",
        "level": 2
      },
      {
        "id": "s3",
        "text": "三、说清楚你要什么",
        "level": 2
      },
      {
        "id": "s4",
        "text": "四、常见需求，直接抄",
        "level": 2
      },
      {
        "id": "s5",
        "text": "五、AI 写好的代码怎么跑起来",
        "level": 2
      },
      {
        "id": "s6",
        "text": "⚠️ 不能双击打开 HTML 文件",
        "level": 3
      },
      {
        "id": "s7",
        "text": "六、出问题了怎么跟 AI 说",
        "level": 2
      },
      {
        "id": "s8",
        "text": "七、几件事先说明白，免得白折腾",
        "level": 2
      },
      {
        "id": "s9",
        "text": "八、想深入了解",
        "level": 2
      }
    ]
  },
  {
    "slug": "readme",
    "label": "SDK 说明与排错",
    "summary": "SDK 完整说明、API 一览和排错对照表。连不上设备时先查这份。",
    "forAi": false,
    "file": "README.md",
    "title": "Shroom SDK",
    "html": "<p>压力传感器最小 SDK。<strong>只做三件事</strong>：连接串口 → 拿到数据 → 画成图。</p>\n<p>曲线、回放、报表、算法这些都不在里面，那是你（或者 AI）在这个基础上写的部分。</p>\n<blockquote>\n<p><strong>不想自己写代码？</strong> 看 <a href=\"%E6%80%8E%E4%B9%88%E7%94%A8AI%E5%BC%80%E5%8F%91.md\">怎么用AI开发.md</a>——把 <a href=\"AI-CONTEXT.md\">AI-CONTEXT.md</a>\n丢给 AI，然后说人话描述你要什么就行。</p>\n</blockquote>\n<p><strong>Windows / macOS / Linux 都支持</strong>，SDK 是同一份，不分平台版本。\n差别只在两件事：启动脚本（<code>start-demo.bat</code> vs <code>sh start.sh</code>）和串口驱动/权限（见文末排错表）。\n浏览器方案要 Chrome 或 Edge —— Safari 和 Firefox 三个平台上都没有 Web Serial。</p>\n<hr>\n<h2 id=\"s1\">30 秒跑起来</h2>\n<p><strong>想连真实设备 → 双击 <code>start-demo.bat</code></strong>（macOS / Linux 用 <code>sh start.sh</code>）。\n它会自己切到正确目录、检查 Node、起服务器并打开浏览器；出错也不会一闪就关，能看清报错。\n需要 Node 18 以上（<code>node -v</code> 查一下，没有就去 <a href=\"https://nodejs.org\">https://nodejs.org</a> 装）。</p>\n<p><strong>只想先看看长什么样 → 双击 <code>web/index.html</code></strong>，什么都不用装。\n模拟数据、热力图、配色切换都能正常跑，只有「连接设备」是灰的。</p>\n<blockquote>\n<p>为什么双击连不了设备？浏览器规定串口只在 <code>https</code> 或 <code>localhost</code> 下开放\n（<code>navigator.serial</code> 在规范里标了 <code>SecureContext</code>），<code>file://</code> 不在白名单里。\n这是浏览器的硬规定，任何打包方式都绕不过去，只能用上面那个 <code>start-demo.bat</code>。</p>\n</blockquote>\n<p>命令行开法：</p>\n<pre><code class=\"language-bash\"># 浏览器示例（推荐先看这个）\nnode start.mjs\n# 会自动打开浏览器；端口被占用会自动顺延到 5179、5180…\n\n# 终端示例\nnode node/demo.js            # 模拟数据\nnode node/demo.js --list     # 列出串口\nnode node/demo.js COM3       # 连真实设备\n</code></pre>\n<p>macOS / Linux 可以用 <code>sh start.sh</code>。</p>\n<hr>\n<h2 id=\"s2\">浏览器里用</h2>\n<pre><code class=\"language-js\">import { Shroom } from &#39;./sdk/web/index.js&#39;;\n\nconst heatmap = Shroom.createHeatmap(&#39;#view&#39;);   // #view 是一个 &lt;canvas&gt;\n\ndocument.querySelector(&#39;#btn&#39;).onclick = async () =&gt; {\n  const device = await Shroom.connect({ baudRate: 1000000 });\n  device.onFrame((frame) =&gt; heatmap.render(frame));\n};\n</code></pre>\n<p>三行就是全部。<code>Shroom.connect()</code> <strong>必须写在点击回调里</strong>——浏览器规定串口只能由用户手势触发。</p>\n<p>没有硬件就把 <code>Shroom.connect()</code> 换成 <code>Shroom.mock()</code>，其余代码一个字不用改：</p>\n<pre><code class=\"language-js\">const device = Shroom.mock({ rows: 32, cols: 32, fps: 30 });\n</code></pre>\n<p><strong>浏览器要求</strong>：Chrome 或 Edge（Safari / Firefox 没有 Web Serial），页面是 https 或 localhost。\n用 <code>Shroom.isSupported()</code> 提前判断。</p>\n<hr>\n<h2 id=\"s3\">Node / Electron 里用</h2>\n<pre><code class=\"language-bash\">npm i serialport\n</code></pre>\n<pre><code class=\"language-js\">import { Shroom } from &#39;./sdk/node/index.js&#39;;\n\nconst device = await Shroom.connect({ path: &#39;COM3&#39;, baudRate: 1000000 });\ndevice.onFrame((frame) =&gt; {\n  console.log(Shroom.renderAscii(frame));   // 终端里的彩色热力图\n});\n</code></pre>\n<hr>\n<h2 id=\"s4\">Frame：唯一的数据结构</h2>\n<p>不管数据从哪来（浏览器串口、Node 串口、模拟），<code>onFrame</code> 拿到的都是同一个东西：</p>\n<pre><code class=\"language-js\">{\n  raw,        // Uint8Array    原始 ADC 值 0~255，一个点一个字节\n  values,     // Float32Array  归一化到 0~1，长度 = rows * cols\n  rows, cols, // 矩阵尺寸\n  min, max, avg,  // 归一化后的极值和均值\n  area,       // 受力点数（超过阈值的点）\n  center,     // { x, y } 压力重心，都是 0~1\n  timestamp,  // 毫秒时间戳\n}\n</code></pre>\n<p><code>values</code> 是 0~1 的<strong>相对值</strong>，不是物理单位。要换算成 kPa 需要每台设备的标定数据，\n不在这个 SDK 里。</p>\n<p>要按行列取某个点：</p>\n<pre><code class=\"language-js\">const v = frame.values[y * frame.cols + x];\n</code></pre>\n<hr>\n<h2 id=\"s5\">API</h2>\n<h3 id=\"s6\">浏览器（<code>web/index.js</code>）</h3>\n<table>\n<thead>\n<tr>\n<th>方法</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>Shroom.connect(options?)</code></td>\n<td>连接串口，返回 <code>Promise&lt;Device&gt;</code>。必须在用户点击里调用</td>\n</tr>\n<tr>\n<td><code>Shroom.mock(options?)</code></td>\n<td>模拟设备，接口与 <code>connect()</code> 完全一致</td>\n</tr>\n<tr>\n<td><code>Shroom.createHeatmap(canvas, options?)</code></td>\n<td>在 canvas 上创建热力图</td>\n</tr>\n<tr>\n<td><code>Shroom.isSupported()</code></td>\n<td>当前浏览器是否支持串口</td>\n</tr>\n</tbody></table>\n<h3 id=\"s7\">Node（<code>node/index.js</code>）</h3>\n<table>\n<thead>\n<tr>\n<th>方法</th>\n<th>说明</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>Shroom.connect(options?)</code></td>\n<td>连接串口，<code>options.path</code> 不填就自动挑第一个</td>\n</tr>\n<tr>\n<td><code>Shroom.listPorts()</code></td>\n<td>列出可用串口</td>\n</tr>\n<tr>\n<td><code>Shroom.mock(options?)</code></td>\n<td>模拟设备</td>\n</tr>\n<tr>\n<td><code>Shroom.renderAscii(frame, options?)</code></td>\n<td>把一帧画成终端里的彩色方块</td>\n</tr>\n</tbody></table>\n<h3 id=\"s8\">Device</h3>\n<pre><code class=\"language-js\">device.info                    // { source, rows, cols, baudRate | path | fps }\nconst off = device.onFrame(fn) // 订阅，返回取消订阅的函数\ndevice.bytesReceived           // 收到的原始字节数（串口设备才有）\ndevice.frameCount              // 成功切出的帧数（串口设备才有）\ndevice.droppedCount            // 丢掉的脏帧数，一直涨说明参数配错了\ndevice.frameLength             // 锁定下来的帧长（字节），还没锁上是 0\nawait device.close()           // 断开\n</code></pre>\n<h3 id=\"s9\">Heatmap</h3>\n<pre><code class=\"language-js\">const heatmap = Shroom.createHeatmap(&#39;#view&#39;, { mode: &#39;dots&#39; })\nheatmap.render(frame)\nheatmap.setOptions({ colormap: &#39;jetWhite&#39;, gain: 2, dotSize: 1.4 })\nheatmap.clear()\n</code></pre>\n<p><strong>三种画法</strong>：</p>\n<table>\n<thead>\n<tr>\n<th>mode</th>\n<th>长什么样</th>\n<th>适合</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>&#39;dots&#39;</code>（默认）</td>\n<td>带透视的点阵，没受力的地方是一片蓝色底噪，受力的地方顶起来并变绿变黄变红</td>\n<td>和 Shroom 桌面端一样的观感</td>\n</tr>\n<tr>\n<td><code>&#39;heat&#39;</code></td>\n<td>每个传感点一个圆斑，边缘淡出，叠加成一片</td>\n<td>看整体压力分布</td>\n</tr>\n<tr>\n<td><code>&#39;grid&#39;</code></td>\n<td>一个点一个方格</td>\n<td>看原始矩阵、调试尺寸</td>\n</tr>\n</tbody></table>\n<p>点阵那条链路和桌面端是同一套：把每个传感点甩到一个更密的格子上 → 高斯摊开 →\n逐点做时间柔化 → 按值定高度、按值取颜色。<strong>每个格点都画</strong>，包括没受力的，\n它们落在 jet 的最低档正好是纯蓝，那片蓝底就是点阵的底子。整个过程只用 canvas，\n不依赖 Three.js。</p>\n<p>其他参数：<code>colormap</code> 可选 <code>&#39;jet&#39;</code>（彩虹）、<code>&#39;jetWhite&#39;</code>（白底）、<code>&#39;grey&#39;</code>（灰度），\n也可以直接传自己的函数 <code>(t) =&gt; [r, g, b]</code>，<code>t</code> 是 0~1；\n<code>gain</code> 是显示增益，信号弱时调大；<code>dotSize</code> 是点 / 圆斑的大小倍数；\n<code>relief</code> 是点阵起伏高度倍数（给 <code>0</code> 就是平的俯视点阵）；\n<code>tilt</code> 是俯视角（度，默认 70）——<strong>嫌点阵看着太扁就调它</strong>，越大越像正俯视、\n方阵看过去越方，示例页面上有个「视角」滑块可以直接拖；\n<code>flipY</code> 决定数据第一行画在近处还是远处（默认 <code>true</code> = 近处）——<strong>按下面却是上面鼓起来\n就把它设成 <code>false</code></strong>，示例页面上是「前后翻转」那个勾。</p>\n<p><code>render()</code> 只是记下这一帧，实际绘制放在下一个屏幕刷新周期。所以数据来得再快\n（串口 1000000 波特下一秒上百帧）也不会多画一次，画面不会闪，CPU 也不会白烧。</p>\n<hr>\n<h2 id=\"s10\">连接参数</h2>\n<pre><code class=\"language-js\">await Shroom.connect({\n  baudRate: 1000000,                       // 默认 1000000\n  delimiter: [0xAA, 0x55, 0x03, 0x99],    // 帧分隔符，默认就是这个\n  rows: 32, cols: 32,                     // 不填按方阵推断：1024 个点 → 32×32\n  fullScale: 255,                         // 满量程\n  threshold: 0.02,                        // 算 area / center 时的有效点阈值\n  lockLength: true,                       // 锁定帧长，默认开，见下\n});\n</code></pre>\n<p><strong>关于 <code>lockLength</code>。</strong> 分隔符只有 4 个字节，数据里迟早会撞出一模一样的一串\n<code>AA 55 03 99</code>，于是切出一个长度不对的短帧。这种长度多半不是完全平方数，\n解码时会退化成 <code>1×N</code>——画面就在方阵和一条横线之间来回闪。\n所以切帧器默认会：连着 3 帧长度一致就把这个长度锁死，之后长度对不上的一律当脏帧丢掉；\n万一锁错了（比如开头第一帧本身就是残的），另一个长度连着来 12 次就改锁它，能自己纠回来。\n<code>device.frameLength</code> 能看到锁在了多少字节。真要收变长帧就传 <code>lockLength: false</code>。</p>\n<p><strong>点「连接设备」没反应？</strong> 对照下面这张表：</p>\n<table>\n<thead>\n<tr>\n<th>现象</th>\n<th>原因</th>\n<th>怎么办</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>「连接设备」是灰的，页面顶上有黄条</td>\n<td>你是双击 <code>web/index.html</code> 打开的，<code>file://</code> 下没有串口</td>\n<td>改用 <code>start-demo.bat</code></td>\n</tr>\n<tr>\n<td>页面一片空白 / 样式全乱</td>\n<td>浏览器太老</td>\n<td>换新版 Chrome 或 Edge</td>\n</tr>\n<tr>\n<td><code>localhost:5178</code> 打不开</td>\n<td>服务器没起来</td>\n<td>见下面「服务器起不来」</td>\n</tr>\n<tr>\n<td>提示「不是安全上下文」</td>\n<td>页面地址不是 https 或 localhost</td>\n<td>换成 localhost，或给站点配 https</td>\n</tr>\n<tr>\n<td>提示「没有 Web Serial API」</td>\n<td>浏览器不支持（Safari / Firefox / 部分国产壳浏览器）</td>\n<td>换 Chrome 或 Edge</td>\n</tr>\n<tr>\n<td>弹了框但列表是空的</td>\n<td>设备没插好，或缺 USB 串口驱动</td>\n<td>见下面「按系统看驱动与权限」</td>\n</tr>\n<tr>\n<td>点了「连接设备」直接说「你取消了选择」</td>\n<td>弹框被你关掉了</td>\n<td>重新点，在列表里选中设备再确认</td>\n</tr>\n</tbody></table>\n<p><strong>按系统看驱动与权限</strong>（列表是空的、或 Node 端 <code>listPorts()</code> 返回空时看这里）：</p>\n<table>\n<thead>\n<tr>\n<th>系统</th>\n<th>要做的事</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>Windows</td>\n<td>装 CH341SER 通用驱动，然后在<strong>设备管理器 → 端口 (COM 和 LPT)</strong> 里确认出现了 COM 口</td>\n</tr>\n<tr>\n<td>macOS</td>\n<td>较新的系统自带 CH34x 驱动，插上后应能看到 <code>/dev/tty.usbserial-*</code>（终端里 <code>ls /dev/tty.*</code> 查）。没有就装芯片厂商驱动，装完要在「系统设置 → 隐私与安全性」里放行</td>\n</tr>\n<tr>\n<td>Linux</td>\n<td>驱动通常在内核里，但<strong>默认用户没有串口权限</strong>：打开设备会报 <code>Permission denied</code>。执行 <code>sudo usermod -aG dialout $USER</code> 后<strong>重新登录</strong>（<code>newgrp dialout</code> 可临时生效）。设备名一般是 <code>/dev/ttyUSB0</code> 或 <code>/dev/ttyACM0</code></td>\n</tr>\n</tbody></table>\n<blockquote>\n<p>Linux 上还有一个坑：某些发行版的 <code>brltty</code> 服务会把 CH340 抢走当盲文设备。\n插上后 <code>/dev/ttyUSB0</code> 一闪就消失的话，<code>sudo systemctl stop brltty</code> 试一下。</p>\n</blockquote>\n<p><strong>服务器起不来 / <code>localhost:5178</code> 打不开？</strong> 先看运行 <code>node start.mjs</code> 的那个窗口打印了什么：</p>\n<table>\n<thead>\n<tr>\n<th>窗口里的提示</th>\n<th>意思</th>\n<th>怎么办</th>\n</tr>\n</thead>\n<tbody><tr>\n<td><code>这个目录下没有 web/index.html</code></td>\n<td>你在外层文件夹里跑的</td>\n<td>解压出来常常是 <code>shroom-sdk\\shroom-sdk\\</code>，要 cd 到<strong>里面</strong>那层</td>\n</tr>\n<tr>\n<td><code>&#39;node&#39; 不是内部或外部命令</code></td>\n<td>没装 Node 或没进 PATH</td>\n<td>装 Node.js 18+，装完<strong>重开</strong>一个终端窗口</td>\n</tr>\n<tr>\n<td><code>端口 5178 被占用，换 5179 试试</code></td>\n<td>端口冲突</td>\n<td>按它最后打印的那个地址开</td>\n</tr>\n<tr>\n<td>窗口一闪就没了</td>\n<td>双击了 <code>start.mjs</code></td>\n<td>双击 <code>start-demo.bat</code>，或在终端里跑</td>\n</tr>\n<tr>\n<td>什么都没打印</td>\n<td>命令没真正执行</td>\n<td>确认你按了回车、且当前目录下有 <code>start.mjs</code>（<code>dir</code> 看一下）</td>\n</tr>\n</tbody></table>\n<p>服务器起来的标志是窗口里出现 <code>Shroom SDK 示例已启动：http://localhost:xxxx</code>，<strong>并且这个窗口不能关</strong>。关掉窗口服务器就停了，页面自然打不开。</p>\n<p><strong>连上了但没画面 / 画面是花的？</strong> 示例页面右下角有一行诊断数字，照着它判断：</p>\n<table>\n<thead>\n<tr>\n<th>诊断</th>\n<th>说明</th>\n<th>怎么办</th>\n</tr>\n</thead>\n<tbody><tr>\n<td>字节 = 0</td>\n<td>设备根本没往外发数据，和解析无关</td>\n<td>确认选对了 COM 口；设备是否需要先发启动指令；换根数据线</td>\n</tr>\n<tr>\n<td>字节在涨，帧 = 0</td>\n<td>数据来了但切不出帧 —— <strong>波特率或分隔符不对</strong></td>\n<td>换波特率：<code>1000000</code>（默认）、<code>921600</code>（手套 / 机器人）、<code>1500000</code>、<code>3000000</code>、<code>115200</code></td>\n</tr>\n<tr>\n<td>帧在涨，画面花</td>\n<td>帧切对了但矩阵尺寸不对</td>\n<td>显式指定 <code>rows</code> / <code>cols</code>，别靠方阵推断</td>\n</tr>\n<tr>\n<td>画面变成一条横线</td>\n<td>帧长不是完全平方数，推不出方阵</td>\n<td>显式指定 <code>rows</code> / <code>cols</code>；<code>device.frameLength</code> 就是实际帧长</td>\n</tr>\n<tr>\n<td>帧在涨，丢弃也在涨</td>\n<td>数据里撞出了假分隔符，被帧长锁定挡掉了</td>\n<td>正常现象，画面稳就不用管；一直猛涨才是波特率不稳，换一档试试</td>\n</tr>\n</tbody></table>\n<p>代码里对应的是这三个计数：</p>\n<pre><code class=\"language-js\">device.bytesReceived   // 串口收到的原始字节数\ndevice.frameCount      // 成功切出的帧数\ndevice.droppedCount    // 长度不合法被丢掉的帧数\ndevice.frameLength     // 锁定下来的帧长（字节）\n</code></pre>\n<hr>\n<h2 id=\"s11\">目录</h2>\n<pre><code>sdk/\n├─ core/          纯逻辑，浏览器和 Node 共用，不碰任何设备 API\n│  ├─ framer.js     字节流 → 一帧一帧\n│  ├─ frame.js      一帧字节 → Frame 对象\n│  ├─ colormap.js   0~1 → 颜色\n│  ├─ device.js     帧订阅\n│  └─ mock.js       模拟数据源\n├─ web/           浏览器：Web Serial + canvas 热力图 + 示例页面\n│  ├─ serial.js         ★ 串口连接就在这里（Web Serial API）\n│  ├─ heatmap.js        canvas 渲染，三种画法\n│  ├─ index.js          模块版入口，正经项目 import 这个\n│  ├─ index.html        示例页面，代码已内联，单独拷到哪都能双击打开\n│  └─ shroom.bundle.js  单文件版（自动生成），给不想用模块的人直接 script 引\n├─ node/          Node / Electron\n│  ├─ serial.js     ★ 串口连接就在这里（serialport）\n│  ├─ ascii.js      终端里的彩色方块图\n│  ├─ demo.js       命令行示例\n│  └─ index.js      Node 入口\n├─ start.mjs      打开浏览器示例用的小服务器\n├─ AI-CONTEXT.md  给 AI 看的说明，直接把这个文件丢给它\n├─ 怎么用AI开发.md  给人看的：怎么把这个 SDK 交给 AI\n└─ index.d.ts     TypeScript 类型\n</code></pre>\n<p><strong>串口连接在哪？</strong> 就两个文件，上面打 ★ 的那两个：浏览器是 <code>web/serial.js</code>，\nNode 是 <code>node/serial.js</code>。两者都是「读到字节 → <code>core/framer.js</code> 切帧 →\n<code>core/frame.js</code> 解码 → 通过 <code>core/device.js</code> 发给你的 <code>onFrame</code>」，\n所以两端拿到的 Frame 完全一样，业务代码可以直接搬。</p>\n<p><code>core/</code> 不依赖任何环境 API，想接别的数据源（WebSocket、蓝牙、文件回放）\n自己调 <code>createFramer()</code> + <code>decodeFrame()</code> 就行，拿到的还是同一个 Frame。</p>\n<hr>\n<h2 id=\"s12\">拿去问 AI</h2>\n<p>SDK 里有一份专门写给 AI 读的说明：<strong><a href=\"AI-CONTEXT.md\">AI-CONTEXT.md</a></strong>。\n把它整个丢给 AI（拖进对话框，或者复制粘贴），然后描述你想做的东西：</p>\n<blockquote>\n<p>帮我做一个页面：用重心控制一个小球移动，压力超过 0.5 就变色。</p>\n</blockquote>\n<p>它里面写全了 API、Frame 结构、几条硬性约束（用户手势、安全上下文、\n数据是 0~1 相对值而不是 kPa）和可以直接抄的例子，AI 照着就能写出跑得起来的代码。</p>\n<p>完整流程见 <strong><a href=\"%E6%80%8E%E4%B9%88%E7%94%A8AI%E5%BC%80%E5%8F%91.md\">怎么用AI开发.md</a></strong> ——包括怎么描述需求、\nAI 写出来的东西怎么跑起来、报错了怎么跟它说。</p>\n<p>SDK 负责把数据稳定地给到你手上，剩下的想怎么玩都行。</p>\n",
    "raw": "# Shroom SDK\n\n压力传感器最小 SDK。**只做三件事**：连接串口 → 拿到数据 → 画成图。\n\n曲线、回放、报表、算法这些都不在里面，那是你（或者 AI）在这个基础上写的部分。\n\n> **不想自己写代码？** 看 [怎么用AI开发.md](怎么用AI开发.md)——把 [AI-CONTEXT.md](AI-CONTEXT.md)\n> 丢给 AI，然后说人话描述你要什么就行。\n\n**Windows / macOS / Linux 都支持**，SDK 是同一份，不分平台版本。\n差别只在两件事：启动脚本（`start-demo.bat` vs `sh start.sh`）和串口驱动/权限（见文末排错表）。\n浏览器方案要 Chrome 或 Edge —— Safari 和 Firefox 三个平台上都没有 Web Serial。\n\n---\n\n## 30 秒跑起来\n\n**想连真实设备 → 双击 `start-demo.bat`**（macOS / Linux 用 `sh start.sh`）。\n它会自己切到正确目录、检查 Node、起服务器并打开浏览器；出错也不会一闪就关，能看清报错。\n需要 Node 18 以上（`node -v` 查一下，没有就去 https://nodejs.org 装）。\n\n**只想先看看长什么样 → 双击 `web/index.html`**，什么都不用装。\n模拟数据、热力图、配色切换都能正常跑，只有「连接设备」是灰的。\n\n> 为什么双击连不了设备？浏览器规定串口只在 `https` 或 `localhost` 下开放\n> （`navigator.serial` 在规范里标了 `SecureContext`），`file://` 不在白名单里。\n> 这是浏览器的硬规定，任何打包方式都绕不过去，只能用上面那个 `start-demo.bat`。\n\n命令行开法：\n\n```bash\n# 浏览器示例（推荐先看这个）\nnode start.mjs\n# 会自动打开浏览器；端口被占用会自动顺延到 5179、5180…\n\n# 终端示例\nnode node/demo.js            # 模拟数据\nnode node/demo.js --list     # 列出串口\nnode node/demo.js COM3       # 连真实设备\n```\n\nmacOS / Linux 可以用 `sh start.sh`。\n\n---\n\n## 浏览器里用\n\n```js\nimport { Shroom } from './sdk/web/index.js';\n\nconst heatmap = Shroom.createHeatmap('#view');   // #view 是一个 <canvas>\n\ndocument.querySelector('#btn').onclick = async () => {\n  const device = await Shroom.connect({ baudRate: 1000000 });\n  device.onFrame((frame) => heatmap.render(frame));\n};\n```\n\n三行就是全部。`Shroom.connect()` **必须写在点击回调里**——浏览器规定串口只能由用户手势触发。\n\n没有硬件就把 `Shroom.connect()` 换成 `Shroom.mock()`，其余代码一个字不用改：\n\n```js\nconst device = Shroom.mock({ rows: 32, cols: 32, fps: 30 });\n```\n\n**浏览器要求**：Chrome 或 Edge（Safari / Firefox 没有 Web Serial），页面是 https 或 localhost。\n用 `Shroom.isSupported()` 提前判断。\n\n---\n\n## Node / Electron 里用\n\n```bash\nnpm i serialport\n```\n\n```js\nimport { Shroom } from './sdk/node/index.js';\n\nconst device = await Shroom.connect({ path: 'COM3', baudRate: 1000000 });\ndevice.onFrame((frame) => {\n  console.log(Shroom.renderAscii(frame));   // 终端里的彩色热力图\n});\n```\n\n---\n\n## Frame：唯一的数据结构\n\n不管数据从哪来（浏览器串口、Node 串口、模拟），`onFrame` 拿到的都是同一个东西：\n\n```js\n{\n  raw,        // Uint8Array    原始 ADC 值 0~255，一个点一个字节\n  values,     // Float32Array  归一化到 0~1，长度 = rows * cols\n  rows, cols, // 矩阵尺寸\n  min, max, avg,  // 归一化后的极值和均值\n  area,       // 受力点数（超过阈值的点）\n  center,     // { x, y } 压力重心，都是 0~1\n  timestamp,  // 毫秒时间戳\n}\n```\n\n`values` 是 0~1 的**相对值**，不是物理单位。要换算成 kPa 需要每台设备的标定数据，\n不在这个 SDK 里。\n\n要按行列取某个点：\n\n```js\nconst v = frame.values[y * frame.cols + x];\n```\n\n---\n\n## API\n\n### 浏览器（`web/index.js`）\n\n| 方法 | 说明 |\n| --- | --- |\n| `Shroom.connect(options?)` | 连接串口，返回 `Promise<Device>`。必须在用户点击里调用 |\n| `Shroom.mock(options?)` | 模拟设备，接口与 `connect()` 完全一致 |\n| `Shroom.createHeatmap(canvas, options?)` | 在 canvas 上创建热力图 |\n| `Shroom.isSupported()` | 当前浏览器是否支持串口 |\n\n### Node（`node/index.js`）\n\n| 方法 | 说明 |\n| --- | --- |\n| `Shroom.connect(options?)` | 连接串口，`options.path` 不填就自动挑第一个 |\n| `Shroom.listPorts()` | 列出可用串口 |\n| `Shroom.mock(options?)` | 模拟设备 |\n| `Shroom.renderAscii(frame, options?)` | 把一帧画成终端里的彩色方块 |\n\n### Device\n\n```js\ndevice.info                    // { source, rows, cols, baudRate | path | fps }\nconst off = device.onFrame(fn) // 订阅，返回取消订阅的函数\ndevice.bytesReceived           // 收到的原始字节数（串口设备才有）\ndevice.frameCount              // 成功切出的帧数（串口设备才有）\ndevice.droppedCount            // 丢掉的脏帧数，一直涨说明参数配错了\ndevice.frameLength             // 锁定下来的帧长（字节），还没锁上是 0\nawait device.close()           // 断开\n```\n\n### Heatmap\n\n```js\nconst heatmap = Shroom.createHeatmap('#view', { mode: 'dots' })\nheatmap.render(frame)\nheatmap.setOptions({ colormap: 'jetWhite', gain: 2, dotSize: 1.4 })\nheatmap.clear()\n```\n\n**三种画法**：\n\n| mode | 长什么样 | 适合 |\n| --- | --- | --- |\n| `'dots'`（默认） | 带透视的点阵，没受力的地方是一片蓝色底噪，受力的地方顶起来并变绿变黄变红 | 和 Shroom 桌面端一样的观感 |\n| `'heat'` | 每个传感点一个圆斑，边缘淡出，叠加成一片 | 看整体压力分布 |\n| `'grid'` | 一个点一个方格 | 看原始矩阵、调试尺寸 |\n\n点阵那条链路和桌面端是同一套：把每个传感点甩到一个更密的格子上 → 高斯摊开 →\n逐点做时间柔化 → 按值定高度、按值取颜色。**每个格点都画**，包括没受力的，\n它们落在 jet 的最低档正好是纯蓝，那片蓝底就是点阵的底子。整个过程只用 canvas，\n不依赖 Three.js。\n\n其他参数：`colormap` 可选 `'jet'`（彩虹）、`'jetWhite'`（白底）、`'grey'`（灰度），\n也可以直接传自己的函数 `(t) => [r, g, b]`，`t` 是 0~1；\n`gain` 是显示增益，信号弱时调大；`dotSize` 是点 / 圆斑的大小倍数；\n`relief` 是点阵起伏高度倍数（给 `0` 就是平的俯视点阵）；\n`tilt` 是俯视角（度，默认 70）——**嫌点阵看着太扁就调它**，越大越像正俯视、\n方阵看过去越方，示例页面上有个「视角」滑块可以直接拖；\n`flipY` 决定数据第一行画在近处还是远处（默认 `true` = 近处）——**按下面却是上面鼓起来\n就把它设成 `false`**，示例页面上是「前后翻转」那个勾。\n\n`render()` 只是记下这一帧，实际绘制放在下一个屏幕刷新周期。所以数据来得再快\n（串口 1000000 波特下一秒上百帧）也不会多画一次，画面不会闪，CPU 也不会白烧。\n\n---\n\n## 连接参数\n\n```js\nawait Shroom.connect({\n  baudRate: 1000000,                       // 默认 1000000\n  delimiter: [0xAA, 0x55, 0x03, 0x99],    // 帧分隔符，默认就是这个\n  rows: 32, cols: 32,                     // 不填按方阵推断：1024 个点 → 32×32\n  fullScale: 255,                         // 满量程\n  threshold: 0.02,                        // 算 area / center 时的有效点阈值\n  lockLength: true,                       // 锁定帧长，默认开，见下\n});\n```\n\n**关于 `lockLength`。** 分隔符只有 4 个字节，数据里迟早会撞出一模一样的一串\n`AA 55 03 99`，于是切出一个长度不对的短帧。这种长度多半不是完全平方数，\n解码时会退化成 `1×N`——画面就在方阵和一条横线之间来回闪。\n所以切帧器默认会：连着 3 帧长度一致就把这个长度锁死，之后长度对不上的一律当脏帧丢掉；\n万一锁错了（比如开头第一帧本身就是残的），另一个长度连着来 12 次就改锁它，能自己纠回来。\n`device.frameLength` 能看到锁在了多少字节。真要收变长帧就传 `lockLength: false`。\n\n**点「连接设备」没反应？** 对照下面这张表：\n\n| 现象 | 原因 | 怎么办 |\n| --- | --- | --- |\n| 「连接设备」是灰的，页面顶上有黄条 | 你是双击 `web/index.html` 打开的，`file://` 下没有串口 | 改用 `start-demo.bat` |\n| 页面一片空白 / 样式全乱 | 浏览器太老 | 换新版 Chrome 或 Edge |\n| `localhost:5178` 打不开 | 服务器没起来 | 见下面「服务器起不来」 |\n| 提示「不是安全上下文」 | 页面地址不是 https 或 localhost | 换成 localhost，或给站点配 https |\n| 提示「没有 Web Serial API」 | 浏览器不支持（Safari / Firefox / 部分国产壳浏览器） | 换 Chrome 或 Edge |\n| 弹了框但列表是空的 | 设备没插好，或缺 USB 串口驱动 | 见下面「按系统看驱动与权限」 |\n| 点了「连接设备」直接说「你取消了选择」 | 弹框被你关掉了 | 重新点，在列表里选中设备再确认 |\n\n**按系统看驱动与权限**（列表是空的、或 Node 端 `listPorts()` 返回空时看这里）：\n\n| 系统 | 要做的事 |\n| --- | --- |\n| Windows | 装 CH341SER 通用驱动，然后在**设备管理器 → 端口 (COM 和 LPT)** 里确认出现了 COM 口 |\n| macOS | 较新的系统自带 CH34x 驱动，插上后应能看到 `/dev/tty.usbserial-*`（终端里 `ls /dev/tty.*` 查）。没有就装芯片厂商驱动，装完要在「系统设置 → 隐私与安全性」里放行 |\n| Linux | 驱动通常在内核里，但**默认用户没有串口权限**：打开设备会报 `Permission denied`。执行 `sudo usermod -aG dialout $USER` 后**重新登录**（`newgrp dialout` 可临时生效）。设备名一般是 `/dev/ttyUSB0` 或 `/dev/ttyACM0` |\n\n> Linux 上还有一个坑：某些发行版的 `brltty` 服务会把 CH340 抢走当盲文设备。\n> 插上后 `/dev/ttyUSB0` 一闪就消失的话，`sudo systemctl stop brltty` 试一下。\n\n**服务器起不来 / `localhost:5178` 打不开？** 先看运行 `node start.mjs` 的那个窗口打印了什么：\n\n| 窗口里的提示 | 意思 | 怎么办 |\n| --- | --- | --- |\n| `这个目录下没有 web/index.html` | 你在外层文件夹里跑的 | 解压出来常常是 `shroom-sdk\\shroom-sdk\\`，要 cd 到**里面**那层 |\n| `'node' 不是内部或外部命令` | 没装 Node 或没进 PATH | 装 Node.js 18+，装完**重开**一个终端窗口 |\n| `端口 5178 被占用，换 5179 试试` | 端口冲突 | 按它最后打印的那个地址开 |\n| 窗口一闪就没了 | 双击了 `start.mjs` | 双击 `start-demo.bat`，或在终端里跑 |\n| 什么都没打印 | 命令没真正执行 | 确认你按了回车、且当前目录下有 `start.mjs`（`dir` 看一下） |\n\n服务器起来的标志是窗口里出现 `Shroom SDK 示例已启动：http://localhost:xxxx`，**并且这个窗口不能关**。关掉窗口服务器就停了，页面自然打不开。\n\n**连上了但没画面 / 画面是花的？** 示例页面右下角有一行诊断数字，照着它判断：\n\n| 诊断 | 说明 | 怎么办 |\n| --- | --- | --- |\n| 字节 = 0 | 设备根本没往外发数据，和解析无关 | 确认选对了 COM 口；设备是否需要先发启动指令；换根数据线 |\n| 字节在涨，帧 = 0 | 数据来了但切不出帧 —— **波特率或分隔符不对** | 换波特率：`1000000`（默认）、`921600`（手套 / 机器人）、`1500000`、`3000000`、`115200` |\n| 帧在涨，画面花 | 帧切对了但矩阵尺寸不对 | 显式指定 `rows` / `cols`，别靠方阵推断 |\n| 画面变成一条横线 | 帧长不是完全平方数，推不出方阵 | 显式指定 `rows` / `cols`；`device.frameLength` 就是实际帧长 |\n| 帧在涨，丢弃也在涨 | 数据里撞出了假分隔符，被帧长锁定挡掉了 | 正常现象，画面稳就不用管；一直猛涨才是波特率不稳，换一档试试 |\n\n代码里对应的是这三个计数：\n\n```js\ndevice.bytesReceived   // 串口收到的原始字节数\ndevice.frameCount      // 成功切出的帧数\ndevice.droppedCount    // 长度不合法被丢掉的帧数\ndevice.frameLength     // 锁定下来的帧长（字节）\n```\n\n---\n\n## 目录\n\n```\nsdk/\n├─ core/          纯逻辑，浏览器和 Node 共用，不碰任何设备 API\n│  ├─ framer.js     字节流 → 一帧一帧\n│  ├─ frame.js      一帧字节 → Frame 对象\n│  ├─ colormap.js   0~1 → 颜色\n│  ├─ device.js     帧订阅\n│  └─ mock.js       模拟数据源\n├─ web/           浏览器：Web Serial + canvas 热力图 + 示例页面\n│  ├─ serial.js         ★ 串口连接就在这里（Web Serial API）\n│  ├─ heatmap.js        canvas 渲染，三种画法\n│  ├─ index.js          模块版入口，正经项目 import 这个\n│  ├─ index.html        示例页面，代码已内联，单独拷到哪都能双击打开\n│  └─ shroom.bundle.js  单文件版（自动生成），给不想用模块的人直接 script 引\n├─ node/          Node / Electron\n│  ├─ serial.js     ★ 串口连接就在这里（serialport）\n│  ├─ ascii.js      终端里的彩色方块图\n│  ├─ demo.js       命令行示例\n│  └─ index.js      Node 入口\n├─ start.mjs      打开浏览器示例用的小服务器\n├─ AI-CONTEXT.md  给 AI 看的说明，直接把这个文件丢给它\n├─ 怎么用AI开发.md  给人看的：怎么把这个 SDK 交给 AI\n└─ index.d.ts     TypeScript 类型\n```\n\n**串口连接在哪？** 就两个文件，上面打 ★ 的那两个：浏览器是 `web/serial.js`，\nNode 是 `node/serial.js`。两者都是「读到字节 → `core/framer.js` 切帧 →\n`core/frame.js` 解码 → 通过 `core/device.js` 发给你的 `onFrame`」，\n所以两端拿到的 Frame 完全一样，业务代码可以直接搬。\n\n`core/` 不依赖任何环境 API，想接别的数据源（WebSocket、蓝牙、文件回放）\n自己调 `createFramer()` + `decodeFrame()` 就行，拿到的还是同一个 Frame。\n\n---\n\n## 拿去问 AI\n\nSDK 里有一份专门写给 AI 读的说明：**[AI-CONTEXT.md](AI-CONTEXT.md)**。\n把它整个丢给 AI（拖进对话框，或者复制粘贴），然后描述你想做的东西：\n\n> 帮我做一个页面：用重心控制一个小球移动，压力超过 0.5 就变色。\n\n它里面写全了 API、Frame 结构、几条硬性约束（用户手势、安全上下文、\n数据是 0~1 相对值而不是 kPa）和可以直接抄的例子，AI 照着就能写出跑得起来的代码。\n\n完整流程见 **[怎么用AI开发.md](怎么用AI开发.md)** ——包括怎么描述需求、\nAI 写出来的东西怎么跑起来、报错了怎么跟它说。\n\nSDK 负责把数据稳定地给到你手上，剩下的想怎么玩都行。\n",
    "toc": [
      {
        "id": "s1",
        "text": "30 秒跑起来",
        "level": 2
      },
      {
        "id": "s2",
        "text": "浏览器里用",
        "level": 2
      },
      {
        "id": "s3",
        "text": "Node / Electron 里用",
        "level": 2
      },
      {
        "id": "s4",
        "text": "Frame：唯一的数据结构",
        "level": 2
      },
      {
        "id": "s5",
        "text": "API",
        "level": 2
      },
      {
        "id": "s6",
        "text": "浏览器（web/index.js）",
        "level": 3
      },
      {
        "id": "s7",
        "text": "Node（node/index.js）",
        "level": 3
      },
      {
        "id": "s8",
        "text": "Device",
        "level": 3
      },
      {
        "id": "s9",
        "text": "Heatmap",
        "level": 3
      },
      {
        "id": "s10",
        "text": "连接参数",
        "level": 2
      },
      {
        "id": "s11",
        "text": "目录",
        "level": 2
      },
      {
        "id": "s12",
        "text": "拿去问 AI",
        "level": 2
      }
    ]
  }
];

export function getDoc(slug: string): DocPage | undefined {
  return docPages.find((page) => page.slug === slug);
}
